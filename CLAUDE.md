# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

Szikora Transz flottakezelő (fleet management) system: a React admin/driver UI on top of a plain PHP (no framework) REST-ish API, backed by MySQL/MariaDB via PDO. Built on the Notus React (Creative Tim) Tailwind admin template — most of `src/components`, `src/assets`, `src/layouts` are template scaffolding, not app-specific code.

Domain modules (mirrored across frontend views, backend interfaces, and API actions): kamion (trucks), potkocsi (trailers), furgon (vans — self-propelled, full parity with kamion; see "Furgon (van)" below), soforok (drivers), karbantartasok (maintenance records), bejelentesek (incident reports — list/create/edit/delete backend is real, but the in-report message thread is still frontend-only fake data, see "Database schema" below), files, email, esemenyek/egyediHataridok (calendar events / custom deadlines — computed on the fly, no dedicated table).

## Commands

Frontend (run from repo root):
- `npm start` — CRA dev server (localhost:3000)
- `npm run build` — production build into `build/` (also strips template license comments via `gulp licenses`)
- `npm test` — CRA/Jest test runner (no test files currently exist in the repo)
- `npm run build:tailwind` (or `npx tailwindcss -i ./src/assets/styles/index.css -o ./src/assets/styles/tailwind.css`) — rebuild `src/assets/styles/tailwind.css` from `src/assets/styles/index.css`. **This is NOT automatic.** `src/index.js` imports the pre-built `tailwind.css` directly (not `index.css`), so CRA does not recompile Tailwind on the fly. Any new utility class used in JSX must be followed by this rebuild, or it silently won't exist in the stylesheet (e.g. an element with `hidden md:flex` can end up permanently hidden if `.md\:flex` was never compiled in).

Backend (PHP, no build step, no composer dependencies):
- `cd backend && php8.2 -S localhost:8000` — run the API locally (see discrepancy note below)

There is no linter config beyond CRA's default `eslintConfig: { "extends": "react-app" }`, and no PHP test suite.

**Részletes, dátumozott munkamenet-naplók** (audit-körök teljes indoklása, élő-tesztelési jegyzőkönyvek) a `CLAUDE-history.md`-be lettek kiszervezve, hogy ez a fájl ne terhelje feleslegesen minden beszélgetés kontextusát — az alábbi szekciók a máig élő architektúrát/konvenciókat/gotchákat tömören tartalmazzák, a teljes narratívát csak igény esetén nyisd meg a history fájlból.

## Architecture

### Frontend routing
`src/index.js` mounts three independent layouts under `react-router-dom` v5: `layouts/Admin.js`, `layouts/User.js`, `layouts/Auth.js`, plus standalone `views/Landing.js` and `views/Profile.js`. Each layout owns its own `<Switch>` of pages from `src/views/{admin,user,auth}/`. There's no route guard component — auth/role gating happens ad hoc inside pages (check `localStorage`/props usage in view files, not a shared context).

**The real login URL is `/auth/login`** (rendered by `layouts/Auth.js`, which redirects bare `/auth` to it). `layouts/Admin.js` also declares a `<Route path="/login" exact component={LoginPage} />` inside its own `<Switch>`, but that's dead code — `Admin` only ever mounts for paths starting with `/admin` (see the top-level `<Route path="/admin" component={Admin} />` in `src/index.js`), so a bare `/login` URL never reaches it and instead falls through to the catch-all `<Redirect from="*" to="/" />` (Landing page). Don't be misled by it when tracing the login flow, and don't "fix" it into a real redirect without checking whether that's actually wanted — it's unreachable, not broken.

`views/auth/Login.js` branches its entire render on `useMediaQuery({ maxWidth: 1023 })` (the same cutoff as the original design's `lg:` branding-column breakpoint): under 1024px it's a full-bleed dark single-card view; at `lg`+ it's the original two-column light layout with the branding copy. These are two genuinely different JSX trees (not one tree reshaped by Tailwind breakpoints) because the page background color, card background/blur, and whether the branding column exists at all all differ structurally, not just by size — don't try to collapse them back into a shared responsive tree.

**Stacking-context gotcha in `layouts/Auth.js`**: `AuthNavbar` (the floating "Főoldal" pill top-left) is `position: absolute; z-index: 50`, a sibling of the routed `<Switch>` content. That `<Switch>` used to be wrapped in `<div className="relative z-10">` — the `z-10` on that wrapper (not `relative` alone — `relative` without an explicit `z-index` doesn't do this) creates its own stacking context, which means everything inside it, including `Login.js`'s own `fixed inset-0 z-[9999]` full-screen overlay, gets flattened to a single z-10 layer from the *outside* stacking context's point of view. Since 10 < 50, `AuthNavbar` always painted on top of Login's supposedly-opaque overlay, no matter how high Login's own z-index was set — a real bug that predates this session's changes but only became visible once Login became a true full-bleed overlay. Fixed by dropping the `z-10` (kept `relative`, which is still needed to stack this div above the section's own absolutely-positioned background/grain-overlay divs via plain DOM order — no `z-index` required for that). If a future full-screen overlay component (another modal-like page, a lightbox, etc.) seems to render *behind* something with a much lower z-index, check every ancestor for an unnecessary `z-*` class creating an intermediate stacking context — the fix is almost always to remove that ancestor's z-index, not to raise the overlay's.

### API communication
All API calls funnel through two helpers in `src/utils/`:
- `fetchAction(action, payload)` — POSTs `{ authHash, action, ...payload }` as JSON to `api.php` and returns the parsed JSON response `{ success, ... }`.
- `downloadFileAction(id, filename)` — same pattern, but decodes a base64 file from the response and triggers a browser download.

The API base URL and `authHash` are hardcoded client-side constants in each of these files (dev: `http://localhost:8001/api.php`, prod: `https://szikora-transz.hu/backend/api.php`). **Note the dev port mismatch**: `todo.txt` documents running the PHP server on port 8000, but the frontend code points at port 8001 — start PHP accordingly (`php8.2 -S localhost:8001`) or update both `src/utils/*.js` files if changing this.

### Backend API dispatch
Single entrypoint `backend/api.php` decodes the JSON POST body and delegates everything to `ApiHandler` (`backend/ApiHandler.php`). `ApiHandler`:
- Declares every valid action + its required parameters in `getActions()` (a flat action-name → required-fields map).
- `validation()` checks the shared `authHash` (from `backend/config.php`), that the action exists, that required params are present, and does ad hoc email validations.
- `process()` is a large `switch` over `$action` that dispatches to methods on `ApiHandler` itself or to the per-domain interface classes required at the top of the file (`backend/interface/*Interface.php` — one per domain module listed above).

To add a new API action: add it to `getActions()` with its required params, add a `case` in `process()`, and implement the logic either inline in `ApiHandler` or in the relevant `interface/*Interface.php` file.

**To add a brand-new `interface/*Interface.php` file** (a new domain module, not just a new action on an existing one), three edits are required in `ApiHandler.php`, not two — it's easy to do the first two and get a fatal error anyway:
1. `require 'interface/xInterface.php';` near the top with the others.
2. Instantiate it (same pattern as the existing `$xInterface = new XInterface($this->db);` block).
3. **Add the new variable name to the single `global $kamionInterface, $potkocsiInterface, ...;` statement inside `process()`** (around line 532). This is the one that's easy to forget — `require` + instantiation alone are not enough, because `process()` only sees interface objects that are explicitly named in that one `global` list. Forgetting step 3 fails as "Call to a member function X() on null", not as a missing-require error, which makes it a confusing failure mode if you don't know to check this list.

`backend/db.php` defines `Database::connect()` (PDO/MySQL, DB name `kamion`) with credentials hardcoded in the class — same pattern in `backend/config.php` for the shared `authHash`. There is no `.env`/secrets layer; secrets live directly in these two PHP files.

### Session & permission model
Every admin-facing action that mutates or reads scoped data resolves identity **server-side from the `sessions` table**, never from client-submitted `kerelmezo_id`/`admin`/`ceg_id` fields (those are accepted for backward-compatible request shape and cross-checked against the resolved session, but never trusted as the source of truth — this was the subject of a full security-audit pass; see git history around `Implement security enhancements and validation checks across various interfaces`). Key helpers in `ApiHandler.php`:
- `resolveKerelmezo($request)` — resolves the caller's `admin` table row from `sessions`, requires an admin-type session (throws otherwise), and computes `is_root`/`ceg_id`. Throws if a client-submitted `ceg_id` doesn't match.
- `resolveSajatSoforId($request)` — the sofőr-session equivalent; requires a `sofor`-type session, returns that sofőr's own id. Used for driver self-service actions (jármű-váltási kérés, etc.) so a driver can't act on another driver's behalf.
- `resolveSajatCegId($request)` — works for **either** session type (admin or sofőr), always returns the real owning company id server-side. Used by actions reachable from both roles (file upload, etc.).
- `requireAdminRole($request)` / `hivoSajatSzerepkoreAdmin($request)` (throwing vs. non-throwing; the non-throwing one was renamed from `kerelmezoAdmin` on 2026-07-19 — the old name read as if it did row-ownership checking, it doesn't) — checks `is_root || szerepkor === 'admin'`, for admin-only fields/actions (e.g. bér visible only to admin, not to a fuvarszervező role viewing the same sofőr record). It does **not** check that a target row belongs to the caller — see the audit note below for why that distinction mattered.
- `requirePermission($request, $modul, $tipus)` — checks the configurable per-role module permission (`Jogosultsagok` admin page) via `MODULE_PERMISSION_MAP` (action → `[modul, tipus]`); `ADMIN_ONLY_ACTIONS` is a flat list of actions that bypass the configurable map entirely and are always admin/root-only (team/role/permission management itself).

**Team members share the `admin` table, not a separate table**: `admin.tulajdonos_admin_id` is `NULL` for the root/owner account and points at the owner's `admin.id` for every csapattag (team member) invited under that company. `ceg_id` (the actual tenant-scoping key used everywhere data is filtered) is **not** the logged-in user's own `admin.id` — it's `is_root ? id : tulajdonos_admin_id`. Any new query/action that scopes by "the current admin's data" must use this resolved `ceg_id`, not the bare session `felhasznalo_id`, or a team member's actions will silently operate on/leak the wrong slice of data (this exact bug class — trusting client-submitted `admin`/`ceg_id` instead of resolving it — was the root cause of the account-takeover and cross-tenant findings fixed in the security audit).

**Client-side auth persistence: `localStorage`, not `sessionStorage` (changed 2026-07-18)**. The `user`/`sessionToken` pair that every `PrivateRoute` (`layouts/Admin.js`, `layouts/User.js`) and `fetchAction`/`downloadFileAction` reads is now written to `localStorage` everywhere (~60 files, all consistent — `sessionStorage` was previously used exclusively for these two keys and nothing else, so this was a safe global rename, not a partial migration). **Why**: `sessionStorage` is scoped to the browsing-context/tab's lifetime, which mobile browsers and standalone-PWA processes (`public/manifest.json`'s `display: "standalone"`) routinely tear down and recreate under memory pressure or after being backgrounded — this silently wiped the "logged in" state even though the backend's own `sessions` row (30-day expiry, `ApiHandler::loginUser()`) was still perfectly valid, forcing a full re-login that looked like a bug but was really just `sessionStorage` doing exactly what it's spec'd to do. `localStorage` persists until an explicit logout (`localStorage.removeItem` calls, now consistent across all logout call sites — `UserDropdown.js` previously forgot to clear `sessionToken`, only `user`, fixed alongside this change) or a real "session expired" response from the backend (`fetchAction.js`'s `handleExpiredSession()`).
- `views/auth/Login.js` now also auto-redirects on mount if `localStorage.getItem("user")` is already present (`is_admin` decides `/admin/dashboard` vs `/user/dashboard`, via `history.replace` so back-navigation can't loop to the login form) — this matters because `manifest.json`'s `start_url` is hardcoded to `/auth/login`, so every fresh PWA launch used to render the login form for a split second (or, pre-fix, force a real re-login) even for an already-authenticated user.
- The backend session itself was **not** changed (still a fixed 30-day expiry from login, no sliding/refresh-on-activity logic, no cleanup cron for stale rows) — the mobile "keeps asking to log in" complaint was fully explained by the client-side storage choice, not the server-side expiry window, so that was left alone rather than widened speculatively.
- Verified locally end-to-end (not just read the code): inserted a real row into the local `sessions` table for `admin.id=1`, drove it through Playwright — direct navigation to `/admin/dashboard` with only `localStorage` populated stays authenticated, `/auth/login` auto-redirects away when already authenticated, and a **brand-new browser context** (`storageState` carrying over only `localStorage`, simulating a killed-and-relaunched mobile app process) still lands on `/admin/dashboard` without hitting the login form — this is the actual scenario that was broken before.

### Security/logic audit és fixek (2026-07-19) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
Egy teljes adversarial review után minden domain-interfész (kamion/potkocsi/furgon/soforok/bejelentesek/szabadsag/helyszin/karbantartasok/tankolas/files) áttért a fenti "resolve server-side" mintára — korábban csak `ugyfelInterface.php`/`csapatInterface.php`/`koltsegInterface.php`/`jarmuValtasInterface.php` tette ezt helyesen; ha egy régebbi kódrészletben client-submitted `id`/`ceg_id`/`admin`/`sofor_id` mezőt látsz közvetlenül SQL-be kötve, az valószínűleg egy azóta javított minta maradványa. Azóta is élő konvenciók ebből a körből: `backend/cron/*.php` mind `PHP_SAPI !== 'cli'` guard mögött fut (webrootön élnek, saját auth nélkül); `koltsegInterface.php` elutasít egy tételt, ha egynél több jármű-FK-t (`kamion_id`/`potkocsi_id`/`furgon_id`) kap egyszerre; a `requestPasswordReset` 3 kérés/15perc rate-limitre van állítva (mindig `success:true`-t ad, account-enumeration elkerülésére); a session-lejárat SQL-oldali `(lejarat < NOW())`-on fut, nem PHP `strtotime()`-on (ld. "PHP/MySQL timezone mismatch" lentebb). **Nyitott, tudatosan nem javított tétel**: a `navEncryptionKey` (`config.php`) ugyanabban a committolt fájlban él, mint amit véd — rotálása minden tenant NAV/GPSmart hitelesítőjének újra-bekérését igényelné, ezért egy jövőbeli koordinált rotáció+újratitkosítás vár rá.

### SQL migration convention
`backend/sql/N.sql` files are numbered, sequential, one-shot-committed migrations (currently up to `27.sql`). **Before creating a new numbered file, check `git status`/`git log` for the most recent one**: if it's still uncommitted, append new schema changes to that SAME file instead of creating `N+1.sql` — only start a new file once the previous one has actually been committed. This is a deliberate, durable convention (explicitly requested), not an oversight — it keeps a single work-in-progress migration file per logical unit of work instead of fragmenting one feature's schema changes across several small numbered files.

### Deployment
`genezio.yaml` configures frontend-only deployment (builds via `npm install && npm run build`, publishes `build/`). The PHP backend is deployed separately (see `build/api_proxy.php` and prod URL `https://szikora-transz.hu/backend/api.php`) and isn't managed by the genezio config.

### Database schema (`kamion` MySQL/MariaDB DB)
Inspected directly via `information_schema` (local DB, see "Local dev environment" below). Key facts not obvious from the PHP code alone:

- **Every table uses the `MyISAM` engine, not InnoDB** — so there are **no real foreign-key constraints** anywhere in the schema, even though several index names look like they should be one (`CarNaplo_carId_fkey` on `kamion_karbantartars.kamion_id`, `Car_rendszam_key` on `kamion.rendszam`, `User_email_key` on `user.email`). Referential integrity (`kamion.admin` → `admin.id`, `kamion_karbantartars.kamion_id` → `kamion.id`, `user.kamion` → `kamion.id`, etc.) is convention-only, enforced by application code, never by the DB. Don't assume a delete/update will cascade or get rejected — it won't, either way.
- Those Prisma-style constraint names (PascalCase model names like `Car`, `CarNaplo`, `User` baked into index names that live on snake_case Hungarian tables like `kamion`, `kamion_karbantartars`) are a strong signal this schema was originally scaffolded via Prisma with English model names, then the actual table names were changed to Hungarian later — the old constraint names never got renamed.
- **Soft delete everywhere**: every table has `torolt ENUM('I','N') NOT NULL DEFAULT 'N'` (`I` = igen/deleted, `N` = nem/not deleted). There's no view or trigger hiding deleted rows — every hand-written query must include `WHERE torolt <> 'I'` (or `= 'N'`) itself, or it'll silently include soft-deleted rows.
- **Per-admin ownership**: `kamion`, `potkocsi`, `furgon`, `kamion_karbantartars`, `potkocsi_karbantartars`, `furgon_karbantartars`, `fajlok`, `egyedi_hataridok` all carry an `admin` column pointing at `admin.id` — each admin account owns its own slice of fleet data; nearly every list/get query in `ApiHandler`/interfaces scopes by `WHERE admin = :id`.
- Table purposes, beyond what's in "Domain modules" above:
  - `admin` — the admin/company-user accounts (not "administrator" in a generic sense — this is *the* login table for admin-role users, parallel to `user`).
  - `user` — actually **sofőrök (drivers)**, despite the generic name; `user.admin` → owning admin, `user.kamion`/`user.furgon` → their currently assigned `kamion.id`/`furgon.id` (mutually exclusive in practice, not enforced by the DB).
  - `kamion.potkocsi` is `NOT NULL` — the schema assumes every truck row references exactly one trailer row at all times (no "no trailer attached" state representable without pointing at some `potkocsi` row). `furgon` has **no** equivalent column — a furgon never tows a semi-trailer.
  - `fajlok` — generic polymorphic attachments table: `tabla` enum (`kamion`/`potkocsi`/`furgon`/`sofor`/`egyeb`/`admin`/`karbantartasok`) + `rowid` (indexed) says which row in which domain table the file belongs to. This is what `getFiles`/`fileUpload`/`deleteFile` actually use.
  - `kamion_fajlok` / `potkocsi_fajlok` — an older, differently-shaped per-domain file table (`session_id`/`client_id`/`fh` columns), currently **0 rows in both** — looks superseded by the generic `fajlok` table above; don't build new file-upload features against these.
  - `egyedi_hataridok` — custom one-off deadlines (`admin`, `leiras`, `datum`).
  - **`bejelentesek` (incident reports) IS a real table with a real CRUD backend** (`backend/interface/bejelentesekInterface.php`: `getBejelentesek`/`newBejelentes`/`saveBejelentesData`/`deleteBejelentes`/`generateKarbantartasFromBejelentes`, all real `SELECT`/`INSERT`/`UPDATE` against the table) — an earlier version of this doc claimed the whole feature was fake/stub scaffolding; that was true once but is no longer accurate for the list/create/edit/delete part. What's still genuinely fake: the `getMessages`/`sendMessage` actions the frontend chat/message-thread UI (`CardBejelentesek.js`) calls **don't exist anywhere in `ApiHandler::getActions()`/`process()`** — only the in-report message THREAD is frontend-only scaffolding with no backing data.
- **The event calendar has no dedicated table.** `ApiHandler::getEsemenyek()` computes the feed on every request by scanning expiry-date columns across `user` (jogsi/szemelyi/gki/adr lejárat), `kamion`/`potkocsi` (műszaki/poroltó/adr/tachográf/emelőhátfal/kötélzet/kaszkó lejárat, plus insurance payment schedules run through `calculateNextPaymentDate()`), then appending `egyedi_hataridok` rows and `kamion_karbantartars`/`potkocsi_karbantartars` maintenance dates — all merged into one flat array. There's nothing to migrate/query directly for "events"; to add a new event source, add another block to `getEsemenyek()`.

### PHP/MySQL timezone mismatch (general gotcha)
MySQL's `NOW()` evaluates in the OS/server timezone (CEST/UTC+2 on this machine); PHP's `date()`/`time()`/`strtotime()` evaluate in whatever `date_default_timezone_get()` returns (UTC on this machine). Any code that fetches a naive `DATETIME` string from MySQL and round-trips it through PHP's `strtotime()`/`time()` to compute an age or delta gets silently corrupted by the timezone skew (hit this building the Piaci árak cache-freshness check — a "refetch if older than 1 hour" comparison was always evaluating false because of a consistent ~2 hour offset). **Fix pattern: compute time deltas in SQL** (`TIMESTAMPDIFF(SECOND, some_datetime_col, NOW())`) instead of pulling the DATETIME into PHP and comparing there — both sides then evaluate against MySQL's own clock consistently.

### External data integrations
- **NAV Online Számla** (`backend/interface/navSzamlaInterface.php`, `backend/NavSzamlaClient.php`) — per-company technical-user credentials stored (encrypted) in `nav_szamla_beallitasok`, keyed by `ceg_id` (company-level, not per-login — see "Session & permission model" above for why team members share a `ceg_id`, not their own `admin.id`). Manual query-and-approve workflow (not automatic daily sync, by deliberate choice): admin queries a date range, reviews a digest list of NAV-reported invoices (OUTBOUND→bevétel, INBOUND→kiadás), and explicitly selects which to import into `egyeb_koltsegek`. Already-imported invoices are de-duplicated by matching `szamlaszam`. Non-HUF invoices **are** imported (see "Deviza" below for how) — NAV always supplies a HUF-equivalent per invoice regardless of billing currency.
- **Piaci árak (market prices)** (`backend/interface/piaciArakInterface.php`) — EUR/HUF and USD/HUF from the MNB (Magyar Nemzeti Bank) free SOAP webservice (`mnb.hu/arfolyamok.asmx`, `GetCurrentExchangeRates()`), and a benzinár (fuel price, ESZ-95) scraped from NAV's official monthly reference-price page. There is **no free, key-less, genuinely real-time Hungarian fuel-price API** — the NAV monthly official reference price was chosen as the most authoritative available free source (it's the figure Hungarian businesses actually use for reimbursement), not a live/per-minute price. Results are cached in a non-tenant-scoped `piaci_arak` table (deliberately **not** `ceg_id`-scoped, unlike almost everything else in this schema — exchange rates/fuel prices are identical for every company, so per-tenant storage would be pure duplication) with per-source TTLs (1h for FX, 12h for fuel price) via `frissitVagyOlvas()`. On a fetch failure the last successfully-cached value is served and `frissitve` is left pointing at the last genuinely successful fetch (never bumped for a failed attempt) — graceful degradation, not an error surfaced to the UI. NAV's page embeds its price table as JSON-artifact text (HTML-entity-escaped, containing literal `\n` escape sequences that are *not* real whitespace) — parsing requires `html_entity_decode()` **and** a separate `str_replace(['\n', "\xc2\xa0"], ...)` pass before `is_numeric()`/`trim()`, or the extracted value silently fails to parse as a number. Rendered in the desktop sidebar only (`src/components/Sidebar/PiaciArakPanel.js`, mounted in `Sidebar.js` between the scrollable nav list and the fixed account footer — see "Mobile navigation" below for why that placement keeps it always-visible and automatically mobile-hidden for free) with a 5-minute client poll (cheap, since it's normally just hitting the DB cache).
  - **Market-intelligence rebuild**: a `piaci_arak_elozmeny` table (`kulcs`+`datum` composite PK, written via `ON DUPLICATE KEY UPDATE` on every genuine cache refresh, never backfilled) backs a real day-over-day `valtozasSzazalek` + sparkline in `PiaciArakPanel.js` (icon badges, %-delta pill, inline SVG sparkline once `elozmeny.length >= 2`). A freshly-migrated environment shows no sparklines until a few natural cache-refresh cycles pass — expected, not a bug.
- **GPSmart flottakövető** (`backend/interface/gpsmartInterface.php`) — session-cookie-based HTML scraping (not a real API) via `DOMDocument`. `MAX_UTVONAL_NAPOK = 7` caps route/waybill query ranges to prevent PHP execution-timeout crashes. A vehicle's GPSmart-internal `car_id` is **not** `kamion.id` and isn't stored anywhere — resolved live on every use by matching rendszám.
  - **Napi km-gyorsítótár** (`gpsmart_napi_km`, `backend/cron/gpsmart_km_cache_frissites.php`) backs the Pénzforgalom Ft/km column. Read/write strictly separated: write (route-scraping, slow) only ever runs from the cron, read is a cheap `SUM`/`COUNT` — the Pénzforgalom page can never be slowed/timed out by GPSmart. Cron is bounded+incremental (`MAX_CSOMAG_JARMUVENKENT = 2` seven-day chunks/vehicle/run), never backfills a whole gap in one pass. Today's date is never cached (truck can still be mid-route). Coverage can legitimately be incomplete — `getKoltsegOsszesito()` returns `kmLefedettseg` (% of days cached) and the frontend prefixes the figure with `~` when under 100%.
  - **"Megtett út (ma)" on Flottakövetés** (`GpsmartInterface::lekerdezMegtettUtMa()`) — GPS-route-based (an earlier odometer-diff design was abandoned: the `km` sensor field came back empty for most vehicles). Meaningfully slower than the position poll (fresh login+scrape per vehicle), so it's called only on page load + manual "Frissítés", **never** on the 60s auto-poll. Per-vehicle failures yield `null` for that vehicle only, never a fabricated `0`.
  - **Removed features, deliberately**: fuel-level display (KPI tile, `JarmuReszletek.js` field, low-fuel icon) — explicit user request; if ever re-requested, note the underlying `uzemanyag` field is unreliably `0%` on sensor-less GPS units. The whole "Vezetési idő" (EU 561/2006 driving/rest-time log) feature (2026-07-17, explicit user request) — `src/views/user/VezetesiIdo.js`, admin counterpart, `vezetesiIdoInterface.php`, its cron, all nav entries. DB tables (`vezetesi_naplo`/`gpsmart_vezetesi_javaslat`) deliberately left in place, no destructive migration — same convention as the Fuvarok removal below. If this cron was ever registered in the real server crontab, it still needs unregistering there.
  - **Gotcha, still relevant if `JarmuReszletek.js`/`JarmuLista.js` are touched again**: `JarmuReszletek.js`'s fixed-height panel can silently push its action-button row below the visible scroll area if more fields are added — always re-check it renders without scrolling. `JarmuLista.js`'s desktop table needs `min-w` hints per column (not `table-fixed`+percentages) or the rendszám column truncates.

### Analytics/forecast additions on top of existing modules
A few features were added as new methods on existing interfaces rather than new modules — worth knowing they exist before re-deriving similar logic:
- `tankolasInterface::getFogyasztasElemzes()` — fuel-consumption anomaly detection. Uses **median**, not mean, as the per-truck baseline — a mean is skewed by a single genuine outlier reading, which would falsely flag every normal reading as anomalous too.
- `koltsegInterface::getTervezettKarbantartasBecsult()` — estimated upcoming maintenance cost, folded into `getVarhatoEredmeny()`'s (forecast P&L) breakdown. The flotta-wide average maintenance cost is computed as **two separate SELECTs summed/averaged in PHP**, not a `UNION ALL` — this project's custom SQL linter disallows `UNION` as well as `JOIN` (discovered mid-implementation; the linter warning was a real hit here, unlike the frequent false-positives that trigger on the substring "JOIN" appearing inside unrelated Hungarian comment text).

### MOL üzemanyagkártya PDF import (`backend/interface/molTankolasInterface.php`)
Admin feltölti a MOL "Számla melléklet" nevű, tranzakció-szintű PDF-részletezőt (NEM magát a számlát) — a Pénzforgalom oldal "MOL tankolás import" gombja nyitja a review-modalt, ugyanaz a "digest, admin dönt" kétlépéses minta, mint a Bank/NAV import (`elemezMolTankolasPdf` → előnézet, `alkalmazMolTankolas` → tényleges INSERT csak jóváhagyás után).
- **PDF→szöveg: `pdftotext -layout` (poppler-utils bináris, `exec()`-kel hívva), NEM composer-csomag** — ez a projekt konvenciója szerint egyetlen PHP-függőség sincs, és egy PHP-natív PDF-parser bevezetése nagyobb kockázat lett volna, mint egy már telepített rendszer-bináris meghívása. **Ez éles deploy-kockázat, amit ez a fejlesztői környezet nem tud leellenőrizni**: ha a production PHP-hoston nincs `pdftotext`/poppler-utils telepítve, `MolTankolasInterface::pdfSzovegKinyerese()` `null`-t ad vissza, és `elemezPdf()` egy egyértelmű, a felhasználónak megjelenő hibaüzenettel tér vissza (`success:false`) — sosem fatal hibával —, de a funkció ott ténylegesen nem fog működni, amíg valaki nem telepíti a binárist a szerverre.
- **Sorok kinyerése**: kártyaszám/rendszám-blokkonként (`Kártyaszám: X Rendszám: Y` fejléc-sorok), tranzakciónként 2 sorból (adatsor + helyszín/termék-név sor) — a `pdftotext -layout` oszlop-igazítását kihasználva, egy termék-kategória horgony (`DIESEL`/`AD BLUE pump`/`OTHER`) körül parse-olva, mert a MOL-sorok szerkezete kategóriánként eltérő oszlopszámú (pl. az "OTHER"/éves kártyadíj sorból hiányzik a "Bruttó listaár" oszlop). Csak a liter-mértékegységű (`ME='L'`) sorok kerülnek be a `tankolasok` táblába — a darabszámra menő tételek (éves kártyadíj) kimaradnak.
- **Km-óraállás placeholder-szűrés**: a MOL km-oszlopában "," és néhány (1-3 jegyű, pl. "0"/"1") érték is megfigyelt, megbízhatatlan "nincs olvasat" jelzés, nem valódi óraállás (élő mintaadaton megerősítve: egy jármű egymást követő tankolásainál "1" majd "379161" szerepelt) — csak a ≥1000 értékű, tisztán numerikus km kerül be, minden más `NULL`.
- **Rendszám↔jármű egyeztetés**: a PDF-ben szereplő rendszámot (pl. "RLP018") és a `kamion`/`furgon` táblák `rendszam` oszlopát is csak alfanumerikus, nagybetűs formára hozva ("RLP018" ⇄ "RLP-018") hasonlítjuk össze — a kötőjel/szóköz írásmódja a két oldal közt gyakran eltér. Fel nem ismert rendszámú sornál a frontend egy sárga jelzésű `<select>`-et ajánl fel a jármű kézi kiválasztására; enélkül a sor nem importálható (a "Import" checkbox letiltva marad).
- **De-duplikáció: `tankolasok.mol_slip_id`** (a MOL "Slip ID" oszlopa, tranzakciónként egyedi), `UNIQUE KEY (admin, mol_slip_id)`, NULL a kézi (sofőr-önkiszolgáló) tételeknél — ugyanaz a "több NULL nem ütközik" minta, mint a `user.email`-fixnél. Ez a tényleges védelem egy PDF véletlen kétszeri feltöltése ellen, `elemezPdf()` is ez alapján jelöli meg előre a "Már importálva" sorokat, `alkalmaz()` pedig a `UNIQUE KEY` ütközést (`PDOException` code 23000) is elkapja védekező jelleggel.
- **`tankolasok.sofor_id` NULL-ozhatóvá vált** (korábban `NOT NULL` volt) — a MOL-adatból nem derül ki, ki vezetett, ezt kitalálni tilos (ld. "no fake data" konvenció), ezért a MOL-importált sorok `sofor_id`-je mindig `NULL`. Ennek egyetlen következménye: a sofőr-oldali `getTankolasok($sofor_id)` (driver saját tankolás-listája) ezeket a sorokat nem mutatja — ez szándékos, a MOL-import admin-oldali, flotta-szintű adat.
- **Szándékosan NEM ír az `egyeb_koltsegek` (Pénzforgalom) táblába** — csak a `tankolasok`-ba (liter/km-szintű, a meglévő `getFogyasztasElemzes()` ezt olvassa). Oka: `koltsegInterface::getKoltsegOsszesito()` az "Üzemanyag" összesítőt a `tankolasok.osszeg` ÉS az `egyeb_koltsegek` (`kategoria='uzemanyag'`) összegéből **feltétel nélkül összeadja** — ha ugyanannak a MOL-számlának a forint-értéke mindkét táblába bekerülne, az duplán számolná be a kiadást.
- **NAV↔MOL-import összekötés**: `NavSzamlaInterface::molTankolasSzamlaszamok($ceg_id)` mindkét oldalon bekötve — `lekerdezSzamlak()` kitölti a `mol_tankolasbol_fedve` mezőt (a `mar_importalva`-tól *külön* jelzés) és letiltja az importot, ha igaz; `importalSzamlak()` is újra leellenőrzi szerver-oldalon (defense-in-depth), egy elavult kliensállapot sem tud duplikált sort beszúrni.
- Élő MOL PDF-mintaadaton (34 tranzakció) pontosan visszaadta a MOL számla-összesítő végösszegét — a parser+dedup+rendszám-egyeztetés logikája megbízhatónak igazolódott.

### Deviza (multi-currency) handling in Pénzforgalom
`egyeb_koltsegek` gained `deviza`/`eredeti_osszeg`/`arfolyam` columns, but **`osszeg` remains the single source of truth every report aggregates** — it's always HUF, computed and **frozen** at entry time from that moment's MNB rate. The other three columns are informational-only ("what was actually typed, at what rate") and are **never** recomputed against a later, current rate — this was a deliberate design choice specifically so `koltsegInterface.php`'s existing aggregation SQL (`getKoltsegOsszesito`, etc.) needed **zero changes**, and so a historical report doesn't silently drift as exchange rates move over time.
- **Currency list**: `listaelemek` gained a `'deviza'` `tipus` (same generic per-company list mechanism as `kamion_meret`/`bejelentes_tipus`/etc., ld. `listaInterface.php` `TIPUSOK`), with a **dedicated admin page** (`src/views/admin/Devizak.js`), not a tab inside the generic `Listak.js`. This is deliberate: `Listak.js`'s add-item flow slugifies a free-typed display name into the storage key (`"Kamion méret"` → `kamion_meret`), which is wrong for currencies — the key must be a real, uppercase ISO 4217 code (`EUR`) for the MNB rate lookup to match, not a lowercased slug. `Devizak.js` takes an explicit Kód (forced-uppercase, 3 chars) + Név instead. HUF is seeded `vedett` (protected, undeletable — it's the base/reporting currency); EUR/USD are pre-seeded but deletable.
- **Rate source**: `piaciArakInterface::getArfolyam($devizanem)` — a public method reusing the *same* cache-or-fetch-from-MNB mechanism as the sidebar's EUR/USD widgets (`frissitVagyOlvas()`), but generalized to **any** MNB-quoted currency code, not just the two hardcoded sidebar entries. `koltsegInterface::resolveDevizaOsszeg()` calls it via `global $piaciArakInterface;` (the same cross-interface global-reference pattern `ApiHandler::getEsemenyek()` already uses for `$fuvarInterface`) — no ApiHandler wiring changes were needed for this feature at all, since it reuses 100% pre-existing actions/globals.
- **NAV Online Számla already handled multi-currency correctly before this feature existed** — NAV's own digest response is legally required to include a HUF-equivalent for every invoice regardless of its billing currency (`invoiceNetAmountHUF`/`invoiceVatAmountHUF`), computed by NAV itself at the invoice's issue-date rate, so `navSzamlaInterface.php` never actually excluded or blocked non-HUF invoices. What this feature added on the NAV side: the digest's *original*-currency amount (`invoiceNetAmount`/`invoiceVatAmount`, confirmed present via a live probe) is now also captured, so an imported foreign-currency invoice populates `deviza`/`eredeti_osszeg` too (with `arfolyam` back-computed from NAV's own HUF figure — deliberately **not** a fresh MNB lookup, since NAV's rate for that specific invoice date is more authoritative than today's rate for a historical transaction).

### Removed: Fuvarok / Fuvartervező modules (and fuvar-linked payment tracking)
A `fuvarok` table and matching Fuvarok (list) + Fuvartervező (drag-and-drop dispatch calendar, `react-big-calendar`) admin pages existed at one point, plus a follow-on "kifizetve" (paid/unpaid) flag on each fuvar and a `getUgyfelKintlevoseg()` customer-receivables report built on top of it. **All of this was deliberately removed** (routes, sidebar nav, `fuvarInterface.php`, the `fuvarok`-sourced revenue queries in `koltsegInterface.php`, the `jogosultsagInterface.php` module entry, the `ugyfelek.fizetesi_hatarido_nap`/`fuvarok.kifizetve*` columns) after a product discussion concluded that freight jobs and payment terms in this business are actually negotiated verbally (phone calls), so structured records for them were never reliably kept up to date — a receivables/forecast feature built on data nobody maintains is worse than not having it, since it looks authoritative while actually being wrong. `ugyfelek` (customer contact records — name/address/tax id) was deliberately **kept** as a standalone module; only the fuvar-linked and payment-term parts went. **Consequence for Pénzforgalom**: `koltsegInterface::getKoltsegOsszesito()`'s "Bevétel" (revenue) is now sourced **entirely** from manually-entered/NAV-imported `egyeb_koltsegek` rows with `irany='bevetel'` — there is no automatic revenue source left in the schema. If a future "how much revenue" feature is requested, do not resurrect the old `fuvarok`-based auto-revenue approach without re-confirming the verbal-agreement problem has actually changed. `Naplo.js`'s `TABLA_LABEL.fuvarok: "Fuvar"` was deliberately left in place (not dead code) — historical audit-log rows with `tabla='fuvarok'` from before the removal still exist and need a friendly label to render.

### UX-audit consistency pass
A full-app UX/IA audit (professional "market intelligence dashboard" bar — clarity, hierarchy, semantic color, no decorative noise) led to several cross-cutting consistency fixes, all still in force as conventions for future pages:
- **Unified page headers**: every admin list/detail page now opens with `<PageHeader eyebrow="…" title="…" />` (`src/components/UI/PageHeader.js`). The `eyebrow` mirrors the page's Sidebar nav group, not the page name itself: `Flotta` (Kamionok, Karbantartások), `Csapat` (Sofőrök, Bejelentések), `Partnerek` (Ügyfelek), `Rendszer` (Események), `Pénzügyek` (Pénzforgalom). Reuse this exact eyebrow-to-group mapping for any new page rather than inventing a new label. Bejelentések previously had **zero** page-level title (the audit's most-flagged example) — it now gets one like every other list page.
- **Semantic "warning" tone**: `src/components/Cards/CardStats.js` is the single shared KPI-tile component — `tone` (`"brand"`/`"positive"`/`"neutral"`/`"warning"`/`"danger"`) and `layout` (`"stacked"` vs `"row"`, used by `FlottaKpiKartyak.js`/Pénzforgalom's KPI row). The rule for `warning`/`danger`: only when the underlying count is genuinely > 0 **and actionable** — purely informational counts stay `brand`/`positive`/`neutral` even when non-zero.
- **`text-ink-300` is not an approved color for meaningful text** — audited at ≈2.8:1 contrast, fails WCAG AA. It remains fine for purely decorative icons (search-field magnifying glass, empty-state illustrations, chevrons) where the audit's own scope excluded decoration, but any standalone text conveying information (form-section labels in `FormField.js`'s `FormSection`, `DatePicker.js`'s placeholder/out-of-month-day text, "—" no-data-value placeholders in `Koltsegek.js`/`KihasznaltsagiModal.js`) was bumped to `text-ink-400` (≈4.6:1, passes). Treat `ink-400` as the contrast floor for any new meaningful-but-secondary text; keep `ink-300` reserved for decoration only.
- **Gotcha — Sidebar's mobile floating action buttons can occlude right-aligned page content**: `Sidebar.js` renders two `fixed` circular buttons on every mobile (`md:hidden`) page — a notification bell at `bottom-36 right-4` and a search button at `bottom-20 right-4` (each `h-12 w-12`), together covering roughly the right-most 64px column, ~80–192px above the viewport bottom, **regardless of scroll position**. Any page content that lands in that column at that height (not just near the bottom of the page — this happens on first paint too, if the page's natural content height puts a row there) gets visually/functionally covered. Hit this on Koltsegek.js's "NAV számlák" button (`ml-auto`-pushed to the row's right edge); fixed by adding `pr-16 md:pr-0` to that row's wrapper so nothing renders under the FAB column on mobile (the FABs are `md:hidden`, so no padding is needed back on desktop). If a similar "button/control seems to vanish or gets a stray circle on top of it on mobile" report comes in, check this before assuming a CSS overflow bug.

### Dashboard / Pénzforgalom IA-redesign (2026-07-18) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
Mindkét oldal szerkezetileg átépült egy UX/IA-elemzés nyomán (a jelenlegi struktúra a fenti "UX-audit consistency pass" és a lentebbi "Fejlesztési audit"/"Pénzforgalom UX-audit" körök által tovább is módosult — a **jelenlegi tényleges elrendezésért mindig a `Dashboard.js`/`Koltsegek.js` forráskódját nézd**, ez a bejegyzés csak a döntés hátterét rögzíti). Kulcs-döntések, amik máig érvényesek: a Dashboardon **szándékosan nincs grafikon** (glanceable pillanatkép, az elemzés a Pénzforgalom dolga); a Pénzforgalom kategória-chipjei (Karbantartás/Biztosítás/Fizetés) nem kattinthatók, mert ezek értéke más táblákból (karbantartási rekordok, biztosítási mezők, bérek) számítódik, nem az `egyeb_koltsegek` listából — egy chipre kattintva a szűrt lista sosem egyezne vissza a chip összegével.

### Shared table/list component
Every list page (Kamionok, Potkocsi, Soforok, Fajlok, Esemenyek, Bejelentesek, Karbantartasok) renders through one shared component: `src/components/UI/DataTable.js`. The per-domain `src/components/Table/CardTableFor*.js` files are thin wrappers that just supply `columns`/`rows` — they don't own any layout/styling. `DataTable.js` renders two variants: a card list on mobile (`md:hidden`) and a real `<table>` on desktop (`hidden md:block`). Fix width/spacing/behavior once in `DataTable.js` and it propagates to every list page at once.

### Mobile navigation
`components/Sidebar/Sidebar.js` renders two independent navs, not one that toggles: a desktop-only sidebar (`hidden md:flex`, always expanded, no off-canvas/drawer behavior anymore) and a separate always-visible bottom bar for mobile (`md:hidden`, fixed to the viewport bottom). The bar mixes two kinds of tabs: direct links (`mobileDirectLinks` — Főmenü, Saját adatok: always one tap away, no nesting) and group tabs (`mobileGroups` — Járművek, Alkalmazottak, Egyéb, mirroring the sidebar's section headers) plus a small icon-only logout button. Tapping a group tab expands a short picker list upward, directly above the bar (via `max-h-0` → `max-h-64` transition, not mount/unmount) rather than opening a full-screen menu. `layouts/Admin.js`'s content wrapper reserves bottom padding (`pb-16`) so this bar never covers page content.

The desktop nav has 3 vertical zones inside the `<nav>`: a fixed header (logo + bell), a scrollable nav list (`flex-1 overflow-y-auto`), and a fixed footer (`flex-shrink-0 border-t`, account+logout). Anything placed as a sibling `flex-shrink-0` block between the scrollable list and the footer (e.g. `PiaciArakPanel`, the search bar below) stays permanently visible regardless of nav-list scroll position, and is automatically mobile-hidden for free since it's inside the same `hidden md:flex` parent — this is the pattern to reuse for any future "always-visible sidebar widget."

**Desktop sidebar redesign** (user reference: another in-house app's sidebar — layout/structure only, not its color palette, which stayed brand-blue/white): width is `w-64` (was `w-72`; mirrored in `layouts/Admin.js`'s two `md:left-64` content-offset divs — all three must move together, there's no fourth reference). Search moved out of the header (which is now logo+bell only) into its own always-visible field between `PiaciArakPanel` and the account footer — a button styled like a text input, with a static "Ctrl+K" badge, opening the same `GlobalSearch` overlay as before. A global `keydown` listener in `Sidebar.js` (Ctrl+K / Cmd+K, `e.preventDefault()`) actually backs that badge — don't add a second one elsewhere. `GroupHeader`'s expand/collapse chevron was simplified from a circled icon-button to a plain inline `PiCaretDownLight` to match the referenced design's lighter-weight nav treatment. `PiaciArakPanel.js` was restyled into a denser single-line "ticker" (label — value + tiny up/down caret — unit, no icon badges or sparkline in the row); the %-delta and `idoszakCimke` period caption from the earlier "market intelligence" rebuild (see "External data integrations" above) weren't dropped, just moved into the row's `title` tooltip to keep the row itself compact.

The Bejelentések nav item's unread-count badge does **not** update purely from local state — it refetches on route change and on a 60-second interval, because report status can change from actions elsewhere in the app (or other users) without the Sidebar itself re-rendering. If a similar badge/counter is ever added elsewhere, use the same route-change + interval refetch, not a value only updated at mount.

### Mobile calendar (Dashboard)
`components/Cards/CardCalender.js` branches its entire render on `isMobile` (via `react-responsive`), not just CSS: on mobile it shows a month-grid-only `react-big-calendar` (`views={["month"]}`, no other views, so the view-switch buttons don't even render) with `selectable="ignoreEvents"` + `onSelectSlot`/`onDrillDown` both wired to a single `setSelectedDate` — this is deliberate, since providing `onDrillDown` fully replaces RBC's default "navigate to Day view" behavior, keeping the calendar pinned to month view forever. The selected day's events render in a plain list right below the calendar (`dayPropGetter` adds a `rbc-day-selected` class for the highlight). Desktop keeps the original always-was full view-switching calendar untouched in the same file's second return branch.

### Mobile forms (no popup)
`components/UI/Modal.js` branches on `isMobile` too: desktop keeps the classic fixed-overlay dialog; mobile renders the same content as a **plain in-flow card** (no backdrop, no `fixed`) at whatever point in the JSX tree the call site puts `<Modal>` (all 4 current call sites — Karbantartasok, CardTableForEsemenyek, CardTableForTervezettKarbantartasok, CardTableForPotkocsiTervezettKarbantartasok — put it after their table/list, so opening it means scrolling down; a `scrollIntoView` on open compensates for that).

**Non-obvious browser gotcha hit while building this, and how the final fix is actually structured** — worth reading before touching mobile bottom-clearance again:
- A trailing `margin-bottom` on the last in-flow child of a scrolling container is *not* counted in that container's `scrollHeight` (browsers exclude margins from the scrolling-area box calculation). `mb-6`, `mb-24`, even `mb-96` on a trailing element all produce the *exact same* `scrollHeight` — margin never fixes this, no matter how big.
- A **real sibling block with an explicit height** (e.g. `<div className="h-20" />`, not padding/margin) placed directly after the content that might overflow is what actually reserves usable scroll space — but *only* if it's added **inside the same box whose content might overflow**.
- That "inside" qualifier matters because of a second, separate quirk: page roots built with `h-full` + `flex-col` (used deliberately on Dashboard.js and Karbantartasok.js so DataTable/CardCalender can fill remaining vertical space when content is short) **cap their own rendered height** at the parent's height when `overflow` is left at the default `visible` — so a sibling spacer placed *outside* that box (e.g. a global one in `layouts/Admin.js`, after `<Switch>`) never gets pushed down by that box's own overflow and does nothing for it.
- Net result — **two separate fixes, both still in place, each covering a different page shape**:
  1. `layouts/Admin.js` has a real spacer block (`h-20`, `md:hidden`) right after `<Switch>` — covers "natural flow" pages with no `h-full` root (KamionForm, PotkocsiForm, SoforForm, BejelentesekForm, Settings, and any list page whose `DataTable` isn't in `fill` mode).
  2. Pages whose own root is `h-full flex-col` (Dashboard.js, Karbantartasok.js) additionally carry their **own** trailing spacer *inside* that root — Karbantartasok.js gets one for free from `Modal.js`'s mobile branch (it renders as a `<>`-wrapped sibling next to the card, so it lands inside Karbantartasok's own root), Dashboard.js has an explicit one of its own after the calendar card.
  - If a new `h-full`-rooted mobile page ever has its bottom content clipped by the nav bar again, the Admin.js-level spacer will *not* fix it — add a same-pattern spacer inside that page's own root instead.

### Floating popovers (`components/UI/DatePicker.js`)
The custom calendar dropdown used by every `type="date"` `FormField` renders via `ReactDOM.createPortal(..., document.body)` with `position: fixed` coordinates computed from `getBoundingClientRect()` on open — it does **not** use a plain `absolute` child of its trigger button. This is deliberate, not incidental complexity: an `absolute`-positioned popover nested inside a scrollable container (any `Modal.js` body, any `overflow-y-auto` list) gets silently clipped at that ancestor's edge once it needs to render outside it, which is exactly what happens for a date field near the bottom of a form — the calendar's own bottom (or top, when flipped upward) gets cut off. Portaling to `document.body` with `position: fixed` escapes every ancestor's `overflow`/stacking context, so the panel is always fully paintable regardless of where the field sits.

Same-direction-flip logic still applies (`openUpward` when there isn't `~380px` below the field), now computed once into `top`/`bottom` fixed-pixel coordinates rather than CSS `top-full`/`bottom-full` classes. Two things worth knowing if this file needs touching again:
- **Don't add a scroll/resize listener that recomputes or closes the panel.** It was tried and reintroduced flakiness — Playwright (and likely real browsers) fire spurious `scroll` events from a clicked element's native "scroll into view" adjustment, and recomputing mid-scroll can visually misplace the panel for a frame; closing on scroll made it vanish immediately on some clicks. Position is computed once on open and left alone; click-outside-to-close (checking both the trigger's and the portaled panel's ref, since the panel is no longer a DOM descendant of the trigger once portaled) is enough.
- If you ever debug "the picker opens in the wrong place" via a screenshot again: check whether it's actually the intentional upward-flip (a field near a container's bottom edge legitimately opens a tall panel that visually spans up past *other* fields above it — that's correct floating-dropdown behavior, not a bug) before assuming a positioning regression.

### PWA (installable app)
Full PWA support was added: `public/manifest.json` (proper `icon-192.png`/`icon-512.png`/`icon-maskable-512.png`, all generated locally with ImageMagick — a solid-brand-blue square with an "ST" monogram, since no square app-icon asset existed in the repo, only the wide wordmark `logo.png`/`logo.svg`), `public/apple-touch-icon.png`, iOS-specific meta tags in `public/index.html`, and `src/components/PWA/InstallPrompt.js` (mounted globally in `src/index.js`, outside the router `<Switch>` so it shows on every route) which handles the Chrome/Android `beforeinstallprompt` flow and shows a manual "Add to Home Screen" hint on iOS Safari (which never fires that event).

The actual offline/precache behavior comes from `src/service-worker.js` — **this file's mere existence is what matters**: `react-scripts`' webpack config conditionally adds `WorkboxWebpackPlugin.InjectManifest` only `isEnvProduction && fs.existsSync(swSrc)` (`swSrc` = `src/service-worker.js`). All the `workbox-*` packages it imports (`workbox-core`, `workbox-precaching`, `workbox-routing`, `workbox-strategies`, `workbox-expiration`) are already transitive dependencies of `react-scripts` — nothing extra was installed. `src/serviceWorkerRegistration.js` (pre-existing CRA scaffolding) was already wired up correctly in `src/index.js` (`serviceWorkerRegistration.register()`) but had nothing to register before this, since no `service-worker.js` existed — the registration silently 404'd in every prior production build.

The service worker only activates in production builds served over `https`/`localhost` (`process.env.NODE_ENV === "production"` gate inside `serviceWorkerRegistration.js`) — it will never register under `npm start`. To verify PWA behavior locally: `npm run build`, then serve `build/` statically (e.g. `npx serve -s build -l 4173`) and check `navigator.serviceWorker.getRegistrations()` / DevTools Application tab from there, not from the dev server.

**Gotcha — favicon shown in Google Search results (2026-07-30 fix)**: `icon-192.png`/`icon-512.png`/`icon-maskable-512.png` were at some point regenerated to show the real "SZIKORA TRANSZ" wordmark crop instead of the original "ST" monogram described above, but `favicon.ico` was never regenerated alongside them — it silently stayed on the old "ST" monogram, and that's specifically what Google Search was rendering next to the site in SERPs (users see this, not the actual brand logo). Separately, `public/index.html`'s `<link rel="icon">` pointed at `/logo2.svg` (1600×578, a wide wordmark, **not square**) — Google's favicon requirements (`developers.google.com/search/docs/appearance/favicon-in-search`) mandate a 1:1 square image, so that declaration was likely being silently discarded too. Fixed by regenerating `favicon.ico` from the same square source as `icon-192.png` (`convert icon-192.png -resize <size>x<size> ... && convert <sizes> favicon.ico`), and pointing both `<link rel="icon">` (now `type="image/png"`, `192x192`/`512x512`) and `<link rel="apple-touch-icon">` at `icon-192.png` instead of the non-square `logo.png`/`logo2.svg`. **If any of the square icon files are ever regenerated again, regenerate `favicon.ico` from the same source in the same step** — Google can take days to weeks to recrawl and pick up a favicon change (no way to force it locally; only the URL Inspection Tool's "request indexing" can nudge it).

### Responsive JS logic
`react-responsive` (`useMediaQuery`) is a dependency and is used in both `CardCalender.js` and `Modal.js` (see above) to switch behavior — not just styling — between mobile and desktop. Prefer it over ad hoc `window.innerWidth` listeners for any future JS-level (not just CSS-breakpoint) responsive behavior.

### Design tokens: page background
The main scrollable app background (`layouts/Admin.js`'s "Háttér" layer) is `bg-slate-50` (Tailwind's default cool light gray, available for free since `tailwind.config.js` spreads the full default palette alongside the brand aliases) — it used to be `bg-sand-50` (a warm beige, `#faf8f4`, defined in the `sand` scale). `sand-*` is still used deliberately elsewhere (form field backgrounds, sunken-surface panels like the calendar day-list rows) and wasn't touched — only the page-level background changed, to a more neutral/professional tone. If asked to further reskin the app background, change this one spot in `Admin.js` (and mirror it in `DataTable.js`'s mobile card-list track background, which intentionally matches it) rather than touching `sand-*` itself.

### Karbantartasok mobile filter pattern
`views/admin/Karbantartasok.js` is the reference implementation for "collapsed-by-default filter panel on mobile, unchanged on desktop": `useMediaQuery({ maxWidth: 767 })` sets the initial `filtersOpen` state (`false` on mobile, `true` on desktop, matching pre-existing desktop behavior), a small icon-only button in `PageHeader`'s `action` slot (`md:hidden`) toggles it with an active-filter-count badge, and the original in-card "Szűrők" toggle bar is kept but hidden on mobile (`hidden md:flex`) so desktop is byte-for-byte unchanged. The filter card itself is conditionally `hidden md:block` so mobile doesn't render an empty padded box when collapsed. Reuse this exact pattern (media query for the initial state + `md:hidden`/`hidden md:flex` pair, not two different components) if another list page needs the same "compact on mobile, untouched on desktop" filter treatment.

### Sofőr mobil UX-audit pass
A UX/UI pass on the driver-facing (`layouts/User.js` + `views/user/*.js`) mobile experience. Reusable outcomes:
- **`FormSection` (`components/UI/FormField.js`) has an opt-in `mobileColumns` prop** (default `1`). Set `mobileColumns={2}` only when every field in the section is uniformly short (numbers/dates) — e.g. `Tankolas.js`, `Profil.js`'s "Felhasználó adatok"/"Iratok". Not used on sections mixing short+long fields (would look broken 2-up on narrow screens).
- **`BejelentésUj.js`**: Prioritás field and "Sérülés"/"Baleset" type options are filtered out client-side (`REJECTED_TIPUSOK`) on the driver-facing creation screen only — not from the admin-configurable list or historical records; backend already defaults `prioritas` to `'kozepes'`.
- **`Bejelentesek.js`**: an admin response (`b.admin_valasz`) is surfaced as a green "Válaszolt" pill right next to the status badge, not buried after the description.
- **`Ertesitesek.js` read/unread tracking**: purely client-side (`localStorage` key `sofor_ertesitesek_latott_${user.id}`, never the server). Item ids are category-prefixed (`doc-`/`bej-`/`jv-`); the "new" set is computed against the *previous* visit's stored ids (snapshot-before-overwrite), so the unread indicator stays visible for the whole visit. Reuse this pattern for any future unread-marker.

For headless verification of a **driver** (`sofor`) session specifically (as opposed to the admin pattern below): the `sessions` table needs a row with `felhasznalo_tipus = 'sofor'`, `felhasznalo_id` = the `user.id`; the `sessionStorage` shape mirrors exactly what `loginUser` returns and what `Login.js` stores (`sessionStorage.setItem('user', JSON.stringify(result.user))` + `sessionStorage.setItem('sessionToken', result.token)`), i.e. the full driver row (`id`, `admin` = owning admin id, `name`, `email`, `kamion`, …), not a synthetic `{ id, name, admin: true }` object — that shape is for admin-side verification only (see below) and won't satisfy driver-facing pages/actions that expect real driver fields.

### Long-tail SEO szolgáltatás-oldalak (`src/views/landing/*.js`)
A főoldal (`Landing.js`) mellett önálló, layout nélküli route-ok léteznek konkrét long-tail kulcsszavakra: `/belfoldi-fuvarozas-arajanlat`, `/nemzetkozi-fuvarozas-vamugyintezessel`, `/biztositott-szallitas`, `/expressz-fuvarozas`, `/rendezveny-szallitas`, `/egyedi-arajanlat-fuvarozas`. Mindegyik a közös `components/Landing/ServicePage.js` sablonra épül (fejléc + hero + "miért minket" bullets + referenciák + FAQ + `QuoteForm` + belső linksor + `Footer`), csak a saját tartalmát (eyebrow/H1/intro/bullets/FAQ-részhalmaz + `icon`/`accent` propok egy témás vizuális azonosítóhoz) adja át propként — az oldal fájlja maga csak adat, nem layout. `ScrollToTop.js` kezeli, hogy React Router v5 nem görget fel route-váltáskor. Megosztott építőelemek: `src/data/landingContent.js` (`FEATURES`/`PROCESS_STEPS`/`TESTIMONIALS`/`FAQ_ITEMS`/`SERVICE_PAGES`, `pickFaq()` helper), `components/Landing/QuoteForm.js`, `utils/useSeo.js` (title/meta/canonical felülírás + `FAQPage`/`BreadcrumbList` JSON-LD, unmountkor visszaáll).

**`scripts/prerender.js`** minden `npm run build` után automatikusan lefut (`postbuild` hook, `playwright-chromium` valódi devDependency), az éles deployt is érinti (a `genezio.yaml` is `npm run build`-et hív). **Fail-safe**: soha nem lép ki nem-nulla kóddal — hiányzó csomag/böngésző-hiba/renderelési hiba esetén csak figyelmeztet és a build a szokásos, előrenderelés nélküli állapotban marad, sosem buktatja el a deployt.

**GOTCHA, ha valaha SSR-migráció vagy hasonló útvonal-generálás merülne fel**: a prerenderelt route-ok szándékosan LAPOS `.html` fájlok (`build/belfoldi-fuvarozas-arajanlat.html`), NEM alkönyvtárak — az alkönyvtár-változat élesben 301-es végtelen redirect-hurkot okozott (Apache mod_dir a per nélküli URL-t átirányítja a per-jeles verzióra, és ezt sem `DirectorySlash Off`, sem egy könyvtár-alapú `RewriteRule` nem tudta megkerülni ezen a hoszton). A `.htaccess` egy `RewriteCond %{REQUEST_FILENAME}.html -f` + `RewriteRule ^([^/]+)$ $1.html [L]` (**egyetlen útvonal-szegmens** — egy tágabb `.*` minta végtelen belső-redirect hurkot/500-at okoz egy nem létező alútvonalon) képezi le az extension nélküli URL-t erre. **Ismert, kód-módosítással nem javítható, nyitott ops-tétel**: a production docroot egy korábbi, alkönyvtár-alapú deployból maradt árnyék-könyvtárakat tartalmazhat, amik 403-at okoznak a kanonikus (per nélküli) címen — SSH-hozzáférést igényelne a takarítás, amivel ez a dev környezet nem rendelkezik. Élő ellenőrzéshez mindig `curl -sI`-t használj, trailing slash nélkül — ez a hibaosztály JS-t futtató böngészőből észrevétlen marad.

**Egyéb, máig élő gotcha**: a "Szolgáltatásaink" kártyák teljes kattinthatóságát egy CSS "stretched link" trükk adja (`after:content-[''] after:absolute after:inset-0` a linken + `relative` a kártyán) — Tailwind `before:`/`after:` variánsai csak akkor generálnak tényleges pszeudo-elemet, ha a `content` is explicit be van állítva; enélkül a link csak a szöveg saját, kis területén kattintható. Ha ezt egy jövőbeli komponensnél megint alkalmazod, `getComputedStyle(el, '::after')`-fal ellenőrizd, ne csak vizuálisan.

**Új long-tail oldal felvételéhez 3 helyen kell bekötni** (ugyanaz a mintázat, mint az `ApiHandler.php` új interface-fájljánál): 1) route `src/index.js`-ben, 2) `scripts/prerender.js` `ROUTES_TO_PRERENDER` tömbje, 3) `public/sitemap.xml`. Mindhárom elmaradása esetén az oldal böngészőben működik, de crawler számára "üres" (2.) vagy fel sem fedezhető (3.) marad.

**Fontos üzleti korlát, ami a tartalmi döntéseket vezérli**: a Szikora Transz **nem iparági-specializált** fuvarozó — bármilyen árut szállítanak egyedi igényre. A fenti 6 oldal ezért szándékosan **szolgáltatás-típus szerint** van felépítve, nem iparági vertikumok szerint — egy jövőbeli "élelmiszeripari fuvarozás"/"gyógyszeripari logisztika" jellegű oldal/pozicionálás félrevezető lenne. Ld. memória `project_generalist_positioning`.

### SEO audit javítások (2026-07-17) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
Egy teljes technikai/on-page/tartalmi SEO audit nyomán számos kódszinten javítható elem épült be (`useSeo.js` `Service`/`useNoindex()` bővítés, `Breadcrumb.js`, `LocalBusiness` séma `@id`/`logo`/`inLanguage`, kép-átnevezés+WebP, FAQ heading-szemantika javítás, hiteles kimenő linkek a Nemzetközi/Biztosított oldalakon). **Amit ez a kör tudatosan NEM pótolt** (valós üzleti inputot igényelt volna): névvel jelölt csapattag-bemutatkozás, valós flotta-/km-/ügyfélszám-statisztika, `sameAs` közösségimédia-linkek, valós ügyfélreferenciák a `TESTIMONIALS` fiktív szövegei helyett, tudástár/blog-tartalom. Ezekre csak explicit kérésre, valós adat birtokában érdemes rátérni, ne generálj hozzájuk kitalált adatot.

### Ajánlatkérő form (`components/Landing/QuoteForm.js`) — mezőstruktúra és okai
A CRO/UX-elemzés (2026-07-16) alapján a form 4 mezőről (név/telefon/email/üzenet) egy 9 mezős, de továbbra is **egylépéses** (nem multi-step) formra bővült — a döntés indoklása és a teljes mező-táblázat a korábbi konverzációban készült koncepció-artifactban van, itt csak a kódra vonatkozó lényeg:
- Az "Üzenet" mezőt egy irányított placeholderű "Mit és mennyit szállítanánk?" textarea váltotta, plusz 4 opcionális, alacsony-effort mező (irány/honnan/hová/időzítés) egészíti ki. **Az irány és az időzítés szándékosan gombcsoport (pill), nem `<select>`** — 2-4 opciónál egy dropdown extra megnyitó-kattintást igényelne mobilon, egy gombcsoport egy érintéssel kitölthető és egyszerre látszik minden opció.
- **A backend séma és a `sendAjanlatkeres`/`sendJelentkezes` action nem változott** (`backend/interface/emailInterface.php`, `backend/ApiHandler.php` `saveAjanlatkeres()` → `ajanlatkeresek` tábla): az opcionális mezőket a `QuoteForm.js` `composeMessage()` függvénye egy formázott szöveg-blokká fésüli össze (pl. `"Fuvar iránya: Nemzetközi · Honnan: Budapest · Hová: München"`), ami a szabad szöveges leírás elé kerül, és ez az egész megy a meglévő `message` paraméterben. Ha valaha strukturált (nem csak szöveges) tárolás kell ezekre a mezőkre, az új oszlopot igényel az `ajanlatkeresek` táblában (`backend/sql/N.sql`) és a `saveAjanlatkeres()`/`sendAjanlatkeres()` szignatúrájának bővítését — ez jelenleg tudatosan nincs megcsinálva, mert nincs admin-felületi fogyasztója ezeknek az adatoknak (a `getAjanlatkeresek()` lekérdezés eredményét jelenleg semmilyen `src/views/admin/*` nézet nem jeleníti meg).
- **Mezőszintű hibaüzenetek** natív `required` + `onInvalid`/`setCustomValidity()` mintával működnek (nincs form-library), lásd a fájl tetején az `INVALID_MESSAGES` map-et — ha új kötelező mező kerül be, ide is fel kell venni.
- **GDPR-jelölőnégyzet kötelező**, és a `/adatvedelem` (`src/views/Adatvedelem.js`) oldalra hivatkozik — ez az oldal a "3 helyen bekötendő" long-tail mintát követi (route `index.js`-ben, `prerender.js` `ROUTES_TO_PRERENDER`, `sitemap.xml`), de a `SERVICE_PAGES`/`FEATURES` listákban **nincs benne** (nem szolgáltatás-oldal, nem kap kártyát a főoldalon). Az oldal szövege explicit jelzi, hogy jogi felülvizsgálat nélküli sablon — **ne tekintsd véglegesnek jogi értelemben**, ha valódi éles adatkezelés indul (pl. hírlevél, sütik) rá kell nézetnie egy jogásznak.

### Footer (`components/Footers/Footer.js`) — 4 oszlopos szerkezet
A korábbi 2 blokkos (kapcsolat + térkép) footer helyett 4 oszlop: Bemutatkozás (logó+leírás+3 bizalmi sor), Szolgáltatásaink (a `SERVICE_PAGES` mind a 6 elemét linkeli — ez a legnagyobb SEO-értékű elem, minden oldal láblécében megjelenik), Cég (Kezdőlap/Rólunk/GYIK/Sofőr jelentkezés/Bejelentkezés/Adatvédelem), Kapcsolat (email/telefon/cím + "Árajánlatot kérek" CTA-gomb).
- **`quoteAnchor` prop** vezérli, hova mutat a CTA-gomb, mert a `Footer` minden oldaltípuson (Landing, 6 `ServicePage`, `Adatvedelem`) ugyanaz a komponens, de az ajánlatkérő form horgonya oldaltípusonként más: `ServicePage.js` `id="ajanlatkeres"`-t használ (ez az alapértelmezett `quoteAnchor` érték), `Landing.js` a saját `id="contact"` szekciójára mutat explicit `quoteAnchor="#contact"`-tal, az `Adatvedelem.js`-en pedig (nincs rajta form) `quoteAnchor="/#contact"` navigál vissza a főoldal kapcsolat-szekciójára. Új oldaltípusnál ezt a propot mindig explicit állítsd be, ne hagyatkozz az alapértelmezettre, ha az oldalnak nincs `id="ajanlatkeres"` szekciója.
- A "Rólunk"/"GYIK"/"Sofőr jelentkezés" linkek (`/#about`, `/#gyik`, `/#contact`) mindig a `Landing.js`-re mutatnak, mert ezek a szekciók csak ott léteznek — sima `<a href>`, nem `<Link>`, mert cross-route hash-navigációt kell kezelniük.
- A Google Maps `<iframe>` embed helyett egy `MAPS_LINK` (Google Maps keresés-URL) kattintható cím-link — gyorsabb mobil betöltés, nincs beágyazott iframe minden oldalon.

### Kimenő e-mailek arculata (`backend/interface/emailInterface.php`)
Mind a 4 kimenő e-mail (ajánlatkérés, sofőr jelentkezés, jelszó-visszaállítás, napi lejárat-emlékeztető cron) egyetlen közös, márkázott HTML-váz köré épül a `EmailInterface` osztályon belül — korábban mindegyik saját, egymástól eltérő inline stílusú (és a tényleges brand palettától is eltérő, generikus Tailwind `#2563eb`/`#f97316` színű) HTML-t épített string-konkatenációval, logó/lábléc nélkül.
- **`EmailInterface::layout($badgeLabel, $tone, $contentHtml)`** (private) — táblázat-alapú (nem flexbox/grid, mert Outlook/Gmail nem támogatja), inline stílusú váz: sötétkék (`#000666`, a `public/logo.png` saját háttérszíne — pixelről mintavételezve) fejléc a logóval, fehér tartalom-kártya egy szemantikus jelvénnyel, sötét (`ink-800 #23262b`) lábléc a cég adataival (cím, adószám, telefon, e-mail — ugyanaz, mint a nyilvános `Footer.js`-ben). A logó egy publikus, abszolút URL-ről (`https://szikora-transz.hu/logo.png`) töltődik be, mert e-mail-kliensben nem működik relatív útvonal; a fejléc cellának a logóéval megegyező háttérszíne van fallbackként, ha a kliens blokkolja a képbetöltést (ekkor az `alt` szöveg látszik a márkaszín hátterén, nem egy üres törött kép egy fehér fejlécben).
- **`EmailInterface::TONES`** — a `CardStats.js` `TONE` objektumával megegyező szemantika/hexek (brand/positive/warning/neutral), hogy az e-mail jelvények színe ugyanazt jelentse, mint az admin UI-ban: ajánlatkérés = brand (kék), sofőr jelentkezés = positive (emerald — új jelentkező, pozitív keret), jelszó-visszaállítás = neutral (szürke — biztonsági művelet, szándékosan nem "izgalmas"), lejárat-emlékeztető = warning (amber — ugyanaz a szemantika, mint a Dashboard "Lejáró határidők" csempéjén).
- **Két új publikus metódus** vette át a korábban `ApiHandler.php`-ban/cron scriptben ad hoc HTML-t építő, generikus `sendNotification()`-t hívó kódot: `sendJelszoVisszaallitas($email, $resetUrl)` (a linket egy `layout()`-on belüli `button()` helper márkázott gombként jeleníti meg sima aláhúzott link helyett) és `sendLejaratEmlekezteto($email, $adminName, $items, $windowDays)`. A `sendNotification($to, $subject, $body)` megmaradt jövőbeli automatikus értesítéseknek, de mostantól a hívó által átadott `$body`-t is becsomagolja a közös `layout()`-ba (brand jelvénnyel) — így minden jövőbeli hívó automatikusan örökli az arculatot anélkül, hogy ismernie kellene a layout-ot.
- Élesben lefuttatva (egy `EmailInterface`-t kiterjesztő teszt-osztállyal, ami a `sendEmail()`-t egy mentő closure-re cseréli valódi `mail()` hívás helyett) mind a 4 sablon + egy XSS-teszt (`<script>` a névben — helyesen escape-elve marad) + egy üres lejárati lista szélsőérték helyesen lefutott; a kimenetet Chrome headless screenshottal is ellenőriztük (logó, jelvényszín, lábléc mind helyesen render­elődött).

### Gotcha — kimenő e-mailek mind a 4 típusa spambe ment a `From: agoston@ps.hu` fejléc miatt
Élesben tesztelve (a redesign utáni ellenőrzés során) kiderült, hogy MINDEGYIK kimenő e-mail (ajánlatkérés, jelentkezés, jelszó-visszaállítás, lejárat-cron) a Gmail Spam mappájába érkezett, sosem az Inboxba, annak ellenére hogy a `mail()` PHP-hívás sikert (`true`) jelzett és a `sendEmail()`-ben lévő korábbi hibalogoló ág (`error_log("Email sending failed...")`) sosem futott le — vagyis a levelek ténylegesen elhagyták a szervert, csak a fogadó oldalon minősültek spamnek.
- **Gyökérok, DNS-nyomozással beazonosítva**: a `From` fejléc korábban `agoston@ps.hu` volt (egy, a hosting-szolgáltató/fejlesztői domain címe, amit a szerver SPF-je — `dig TXT ps.hu` — explicit engedélyez erről a szerver-IP-ről). A `ps.hu` domainnek viszont **van egy saját DMARC rekordja `p=quarantine` szabállyal** (`dig TXT _dmarc.ps.hu` → `v=DMARC1; p=quarantine`), miközben a kimenő levelek nincsenek DKIM-mel aláírva. Mivel a `mail()` hívás által használt envelope-from valószínűleg nem egyezik pontosan a `ps.hu` domainnel (DMARC SPF-illesztéshez az envelope-from domainnek és a látható From-domainnek egyeznie kell, nem elég, hogy egy MÁSIK domain SPF-je engedélyezi az IP-t), a DMARC-illesztés elbukik — és a `ps.hu` domain SAJÁT policy-je utasítja a Gmailt (és minden DMARC-ot respektáló fogadót), hogy az ilyen leveleket tegye spambe/quarantine-ba. Ez a policy attól függetlenül lefut, hogy a levél ténylegesen kitől jött — pusztán attól, hogy a From fejléc `@ps.hu`-t mutat.
- **Fix**: a `From` fejléc domainje `agoston@ps.hu`-ról `noreply@szikora-transz.hu`-ra változott (`sendEmail()`, ld. fent) — a `szikora-transz.hu` domainnek **nincs** DMARC rekordja (`dig TXT _dmarc.szikora-transz.hu` üres), és a saját SPF-je (`v=spf1 a mx -all`) explicit engedélyezi ezt a szervert (a domain A rekordja maga ez a szerver-IP). Nincs tehát kényszerítő quarantine-utasítás, amit a Gmailnek követnie kellene, így a levelek sokkal nagyobb eséllyel jutnak el az Inboxba.
- **Ha ez a probléma egy jövőbeli környezetben/domainváltásnál visszatér**: mindig ellenőrizd `dig TXT _dmarc.<From-domain>`-t — ha van rajta `p=quarantine`/`p=reject`, az önmagában elég ok a spambe kerüléshez, függetlenül az SPF-től vagy attól, hogy a `mail()` sikert jelez-e. A tényleges javítás vagy a From-domain cseréje egy DMARC-mentes/lazább policy-jű, a küldő infrastruktúrához illeszkedő domainre, vagy (hosszabb távon, ha a spam-probléma visszatérne) DKIM-aláírás bekötése a szerver oldalán a ténylegesen használt From-domainhez.

### SEO audit fixes, 1-3. kör (2026-07-20) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
Három egymást követő re-audit kör (74→79→82/100 pontszám) a fenti long-tail oldalakon és a `LocalBusiness`/`Service` sémán finomított tovább. Máig élő, konkrét eredmények: **Google Business Profile bekötve** — `Footer.js`'s `MAPS_LINK` egy stabil, cid-alapú Google Maps link (nem generikus keresés-URL), a `LocalBusiness` séma kapott egy `hasMap` mezőt ugyanezzel; a 6 szolgáltatás-oldal FAQ-válaszai és referenciái (`testimonialNames` prop) oldalanként egyediek, nincs többé szó szerinti duplikáció; `ServicePage.js` kapott egy `areaServed` propot (`BelfoldiFuvarozas.js` explicit `["HU"]`-ra szűkítve, a többi oldal `["HU","EU"]` alapértéken maradt); a header-logó `fetchpriority="high"`-t kapott LCP-hint gyanánt (a `ReactDOM.render`/`hydrate` döntéshez **tudatosan nem nyúltunk**, ld. `src/index.js` saját megjegyzése). **Ismert, kód-módosítással nem javítható, nyitott tétel**: a production docroot 403-as/soft-404-es "maradvány-könyvtár" problémája (ld. "Long-tail SEO" fenti GOTCHA-blokkja) SSH-hozzáférést igényel, amivel ez a dev környezet nem rendelkezik — mielőtt bármilyen jövőbeli alkönyvtár-alapú route-megoldás felmerülne, mindig ellenőrizd élőben `curl -sI`-vel, trailing slash nélkül.

### Local dev environment (this machine)
A local MariaDB/MySQL instance is already installed and running with a `kamion` database matching the hardcoded credentials in `backend/db.php` (host `localhost`) — the backend works end-to-end locally without extra setup, no need to mock it. A CRA dev server on port 3000 is also frequently already running in this environment from a previous session; `npm start` will just print "Something is already running on port 3000" in that case — reuse the existing one (it picks up file edits via Fast Refresh) instead of trying to free the port.

For headless browser verification (Playwright/`chromium-cli`) of `/admin/*` pages, the app requires a `user` key in `sessionStorage` (checked by the ad hoc `PrivateRoute` in `layouts/Admin.js`) — `context.addInitScript()` to set `sessionStorage.setItem('user', JSON.stringify({ id, name, admin: true }))` before navigating is enough to bypass the login screen for local-only verification; there's no dedicated test account/seed data documented beyond whatever rows already exist in the local `admin` table.

### Furgon (van) — third, self-propelled fleet vehicle type (full parity with kamion)

A third vehicle category was added alongside `kamion`/`potkocsi`, at the user's explicit request for **full functional parity with kamion** (own adatlap, maintenance history, GPS tracking via GPSmart, fuel log, financial linkage in Pénzforgalom, driver assignment/vehicle-switch requests) — not just a registry entry. A furgon is **self-propelled, like a kamion** (not a towed trailer like `potkocsi`), so it has no `potkocsi`-style towing column and doesn't appear in GPSmart-only-kamion-era code paths without an explicit new branch.

**The general pattern used throughout**: everywhere the codebase already treats kamion/potkocsi as a connected pair (`kamion_id`/`potkocsi_id`, e.g. Pénzforgalom cost linkage, `karbantartasokInterface.php`), furgon became a **third, sibling branch** in the same pattern (3-way ternary/UNION/select). Everywhere something was previously **kamion-only** because potkocsi has no engine/driver (GPSmart tracking, tankolás, sofőr-hozzárendelés, jármű-váltási kérés), furgon became a **new second branch alongside kamion** (not a 3-way — potkocsi still doesn't participate in these).

**Backend**: `backend/interface/furgonInterface.php` is a near-literal mirror of `kamionInterface.php` (minus the `potkocsi` FK), wired into `ApiHandler.php` the standard 3-step way. `furgon`/`furgon_karbantartars` mirror `kamion`/`kamion_karbantartars`. Third-branch extensions: `karbantartasokInterface.php` (3rd UNION ALL branch), `koltsegInterface.php` (furgon breakdown + `furgon_id` on `egyeb_koltsegek`), `listaInterface.php` (own `furgon_meret` list — furgon sizes differ from kamion's, not a reuse), `jogosultsagInterface.php`, global search. Second-branch (kamion-only-because-potkocsi-has-no-engine) extensions: `tankolasInterface.php` (`furgon_id` on `tankolasok`), `jarmuValtasInterface.php`, `gpsmartInterface.php` + its km-cache cron.

**Two real schema bugs were found and fixed during implementation** (both empirically verified against the live local DB, not just reasoned about — see the durable comments left in `backend/sql/28.sql` at each fix site):
1. **`gpsmart_napi_km`'s original nullable `kamion_id`/`furgon_id` pair design was wrong**: MySQL/MariaDB never treats two `NULL`s as equal for `UNIQUE KEY` duplicate-detection, so `INSERT ... ON DUPLICATE KEY UPDATE` against a nullable pair silently inserted a **new duplicate row** on every write instead of updating, which would have double-counted `SUM(km)` in the Ft/km financial report. Fixed by replacing the nullable pair with a single, always-`NOT NULL` `jarmu_tipus ENUM('kamion','furgon')` + `jarmu_id INT` pair and `UNIQUE KEY (jarmu_tipus, jarmu_id, datum)`.
2. **`jarmu_valtas_kerelmek.tipus` turned out to already be a hard `ENUM('kamion','potkocsi')`** (a prior research pass had assumed it was a free string, since the PHP code treats it that way). Inserting `'furgon'` into a MySQL non-strict-mode ENUM column with an unlisted value silently stores an **empty string**, not an error — this corrupted the request's `tipus` and broke rendszám resolution (`getRendszamok()`-based lookup matched no branch) without any visible error until live-tested end-to-end. Fixed by `MODIFY COLUMN tipus enum('kamion','potkocsi','furgon') NOT NULL`. **General lesson reinforced**: an ENUM's actual definition should be checked (`SHOW COLUMNS`), never assumed from how permissively the PHP layer treats the value — this exact failure mode (silent empty-string coercion, no thrown error) is easy to miss without a live end-to-end test.

**Frontend key-collision gotcha, fixed in three places**: any list that groups/keys data by `kamion_id` alone breaks once furgon rows are mixed in, because a furgon row's `kamion_id` is `null` (its own id lives in a separate `furgon_id` field) — multiple furgon rows then collide on the same `undefined`/`null` React key or map key. Fixed by switching to a composite `` `${jarmu_tipus}:${id}` `` key in `CardUzemanyagElemzes.js` (toggle/grouping state), `Flottakovetes.js` (the `megtettUtMa` lookup map built from `gpsmartMegtettUtMa`'s response), and `KihasznaltsagiModal.js` (table row `key`). Any future code that consumes a `kamion_id`/`furgon_id`-pair API response (`getFogyasztasElemzes`, `lekerdezMegtettUtMa`, `getKihasznaltsagiRiport`, `lekerdezPoziciok`) needs the same composite-key treatment if it keys/groups by vehicle.

**Frontend**: full admin CRUD mirroring the kamion module (`Furgonok.js`/`FurgonForm.js`/`CardFurgon.js`/`CardFurgonAdatokForm.js` [no Pótkocsi select]/`CardFurgonFajlok.js`/`CardTableForFurgonok.js`). **Note**: the `CardFurgonEsemenyekForm.js`/karbantartás-tab table components are wired up but, matching pre-existing kamion/potkocsi behavior, no tab button was ever added to reach them — dead/unreachable code inherited from the existing convention, not furgon-specific; the real maintenance-CRUD path is the standalone `Karbantartasok.js` page. Routes: `/admin/furgonok`/`furgonForm`, `/user/furgon-valaszto`. 3-way vehicle-type handling extended into Sidebar, Dashboard's `FlottaOsszesitoStrip`, Koltsegek's "+ Új tétel"/jármű-bontás, Karbantartasok's filter/modal, driver Dashboard's "Aktív jármű" grid.

**`CardUzemanyagElemzes.js` is currently unmounted from the UI** (import/render removed from `Koltsegek.js` by an earlier, unrelated request — file intact for future re-enable) — its furgon support was only verified against the raw API response shape, **not visually in-browser**. Re-check visually if it's ever re-enabled.

**Explicitly not implemented**: furgon cannot tow a `potkocsi` (judged unrealistic for this business); no automatic data migration from pre-existing kamion rows that were actually vans.

## Fejlesztési audit alapján megvalósított batch (2026-07-19) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
Egy 67 tételes fejlesztési-audit artifactból egy 20 elemes köteg valósult meg — az azóta is élő, ténylegesen létező funkciók/fájlok, amiket egy jövőbeli módosításnál érdemes ismerni:
- **`backend/sql/29.sql`**: minden tábla MyISAM→InnoDB + `(admin, torolt)` index 7 táblán. Az app saját DB-felhasználója (`db.php`) csak SELECT/INSERT/UPDATE/DELETE/LOCK TABLES jogú — egy jövőbeli `ALTER TABLE` csak root/DBA-felhasználóval futtatható.
- **`backend/env.php`**: opcionális, `.env`-alapú (git-ből kizárt) titok-felülírás `envOrDefault()`-tal — a hardcoded credentialokat nem rotálja, csak a jövőbeli env-alapú átállást teszi lehetővé.
- Új admin oldalak: `Ajanlatkeresek.js`, `SoforScorecard.js` (`getSoforScorecard()`), `ErtesitesiElozmenyek.js` (`ertesites_naplo` tábla).
- `karbantartasokInterface.php::getKmAlapuEsedekesseg()` — km-alapú (nem csak dátum-alapú) karbantartás-figyelmeztetés a `gpsmart_napi_km` cache-re építve.
- `DataTable.js` opt-in tömeges kijelölés (`selectable`/`bulkActions`) — élesben a Fájlok oldalon.
- `components/UI/Skeleton.js` (`TableSkeleton`) váltotta a generikus Spinnert a `DataTable.js` betöltő állapotában.
- `GpsmartClient.php`/`gpsmartInterface.php` — kliens-példányonkénti (nem kérésekközötti) cookie-cache, cégenkénti egy klienspéldány (`getClient()`).
- `layouts/Admin.js` route-onkénti `React.lazy` (kb. 30 admin-nézet külön chunkba).
- **Web Push** (`backend/WebPushSender.php`, composer-mentes VAPID JWT + aes128gcm, `backend/interface/pushInterface.php`, `push_feliratkozasok` tábla, `PushFeliratkozas.js` a Settings.js-en) — valódi FCM-mel élesben tesztelve.
- **Dark mode** (`tailwind.config.js` `darkMode: 'class'`, `useDarkMode.js`) — a `dark` osztály **szándékosan az `Admin.js` saját gyökér `<div>`-jén ül, NEM a `<html>`-en**, ezért csak az admin felület dark-módosított, a sofőr-felület (`layouts/User.js`) és a nyilvános Landing oldalak nem. **Fontos architekturális gotcha**: bármely `index.js`-szinten, a `<Router>` gyökerén mountolt komponens (pl. `ToastContainer.js` volt ilyen, ld. fix) NEM DOM-őse az `Admin.js` wrappernek, tehát rajta a `dark:` osztályok önmagukban nem tüzelnek — az ilyen komponensnek saját magának kell `useDarkMode()`-ot hívnia és a saját gyökerére tennie a `dark` osztályt (+ route-szűkítést, ha csak admin-nézeten kell). **Screenshot-gotcha**: ebben a sandbox-környezetben `page.screenshot()` néha hamisan fehér háttérrel rajzol ki natív `<input>`/`<select>` elemeket dark módban is, holott a `getComputedStyle` helyes sötét háttért ad vissza — mielőtt CSS-hibát feltételeznél egy screenshot alapján, ellenőrizd `getComputedStyle`/`locator.screenshot()`-tal.
- Kizárt (nem dark-módosított) fájlok: holt komponensek (`CardBarChart.js`, `Dropdowns/*`, `Navbars/IndexNavbar.js`, `views/Profile.js` stb. — import-greppel ellenőrizve, hogy tényleg nem élők) és elérhetetlen (de nem holt) karbantartás-tab komponensek, amikhez sosem lett tab-gomb hozzáadva.
- **WebAuthn** (`backend/CborDecoder.php`, `backend/WebAuthnHelper.php`, `webauthn_hitelesitok`/`webauthn_kihivasok` táblák, `WebAuthnRegisztracio.js`/`WebAuthnQuickLogin.js`) — sofőr-oldali, jelszót NEM kiváltó, csak kiegészítő gyors-belépés, csak ES256/P-256, "none" attesztáció.
- **R51, nyitva maradt**: a korábbi SEO-audit által azonosított 403-as, elhagyott szerver-könyvtárak eltávolítása SSH-hozzáférést igényel, ami ebből a fejlesztői környezetből nem elérhető.

## Teendők akció-központ, sofőr–admin üzenetküldés, bankszámla-kivonat import (2026-07-20) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
- **Teendők akció-központ** (Dashboard) — `ApiHandler::getTeendok()` a meglévő jármű-váltás/nyitott-bejelentés/ajánlatkérés forrásokat fésüli össze egy `TeendokCard`-ba (csak akkor jelenik meg, ha van ténylegesen nyitott tétel), inline jóváhagyás/elutasítás/megnyitás akciókkal.
- **Valódi sofőr–admin üzenetküldés** — `bejelentes_uzenetek` tábla + `bejelentesekInterface::getMessages()`/`sendMessage()` pótolja a korábban dokumentáltan csak frontend-mock üzenetfolyamot. A szerzőt mindig a szerver-oldalon feloldott munkamenet tölti ki, sosem kliens-mező. Sofőr→admin irányban push-értesítés megy (`pushInterface::sendPushAdminnak()`), admin→sofőr irányban nincs (nincs driver-oldali push-infra).
- **Bank-import** (`backend/interface/bankImportInterface.php`) — "digest, admin dönt" kétlépéses minta (`elemezCsv()`/`alkalmaz()`), nincs egyetlen felismert banki CSV-formátum, az admin kézzel rendeli hozzá az oszlopokat. De-duplikáció: `bank_import_tetelek` (`admin`+`tetel_hash` UNIQUE). `egyeb_koltsegek.bank_parositva` jelzi a banki igazoltságot.
- **`Modal.js` portolás + dark-mode fix**: a desktop ág mostantól `ReactDOM.createPortal`-lal `document.body`-ra portol (a korábbi, saját komponensfán belüli renderelés egy széles modalnál a Sidebar alá/fölé keveredett a layout fixed-wrapperje miatt) — ez **minden** meglévő `Modal`-hívást érint. Portolás után a `dark` osztály öröklése megszakadt (ugyanaz a hibaosztály, mint a `ToastContainer`-nél), ezért a `Modal.js` maga hívja a `useDarkMode()`-ot.

## Pénzforgalom UX-audit — Quick Wins + Középtávú kör (2026-07-20) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
Egy 14 találatos UX-audit alapján a Koltsegek.js-en máig élő eredmények: alapértelmezett időszak "Ez a hónap" (nem a teljes év); `bank_parositva` jelvény a Tételek listán; a kategória-összetétel rács csak a Tételek fülön (nem duplikálva a Havi alakulás fülön); letiltott (de látható, tooltippel magyarázott) kategória-opciók a szűrőn (Karbantartás/Biztosítás/Fizetés); "Import ▾" összevont menü a korábban 3 külön gomb (NAV/Bank/MOL) helyett; `DataTable.js` opt-in oszloprendezés (`sortable`, szerver oldali és helyi mód is), élesben a Tételek és a Jármű szerinti bontás fülön; Tétel↔Jármű kereszthivatkozás (`initialSearch` prop a `DataTable`-ön, `key`-alapú remount-mintával — ez a minta újrahasznosítva a Fuvar-figyelmeztetéseknél is, ld. lentebb); KPI %-delta (`KpiDelta` komponens, `getOsszesenGyors()` az előző, azonos hosszú időszak összegére). `Modal.js` kapott `role="dialog"`/`aria-modal`/kezdő-fókuszt (minden meglévő Modal-hívást érint).

## MOL tankolás-import sorok a Tételek listán (2026-07-21) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
A MOL-importból (`mol_slip_id IS NOT NULL`) származó `tankolasok` sorok megjelennek a Pénzforgalom Tételek listáján is — `koltsegInterface.php::getEgyebKoltsegek()` PHP-oldali (nem SQL UNION) egyesítéssel fésüli össze az `egyeb_koltsegek` és a `getMolTankolasTetelek()` forrást; a driver saját, önkiszolgáló (`mol_slip_id IS NULL`) tankolásai szándékosan kimaradnak. Az `id` mező ilyen merge-elt sorokon szándékosan `'tankolas_' . $id` STRING (nem a nyers `tankolasok.id`) — ez különbözteti meg a frontendnek, hogy `updateEgyebKoltseg`/`deleteEgyebKoltseg` NEM alkalmazható rájuk. A KPI-összesítők nem változtak (a `tankolasok.osszeg` már korábban is beleszámított az Üzemanyag kategóriába) — ez csak a LISTA megjelenítését bővíti.

## Tachográf kártya (.ddd) import (2026-07-23) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
**Megjegyzés: a felhasználó ezen a modulon (`Tachograf.js`/`tachografInterface.php`) párhuzamosan, önállóan is dolgozik — ezekhez a fájlokhoz azóta több más munkamenet sem nyúlt hozzá, ne tekintsd ezt a szekciót feltétlenül naprakésznek, mindig ellenőrizd a tényleges kódot.**
Admin-only modul: a sofőr digitális tachográf-kártyájának `.ddd` exportját dekódolja (`backend/DddParser.php`, composer-mentes bináris parser) napi vezetési/pihenő idő + felhasznált jármű riportra. Kulcs FID-ek: `0x0520` Identification, `0x0504` Driver_Activity_Data (ciklikus puffer), `0x0505` Vehicles_Used (**sorban feltöltött tömb, NEM ciklikus puffer** — a fejléc a legújabb rekord indexe; gen1 31 bájt/rekord vs. gen2 48 bájt/rekord, generációnként eltérő rekordméret-választás szükséges, ez volt az egyetlen valódi implementációs hiba ebben a körben). "Digest, admin dönt" import-minta (`elemezTachografDdd`/`alkalmazTachografImport`), `user.tachograf_kartyaszam` auto-felismeréshez. **Nincs automatikus EU 561/2006 jogi szabálysértés-minősítés** — csak egy nyers, explicit "nem jogi minősítés" feliratú küszöbérték-számláló (a heti/kétheti kivételek miatt egy hibás automatikus minősítés rosszabb lenne, mint a hiánya). Egyszerre több `.ddd` fájl is feltölthető, soronként (nem párhuzamosan) feldolgozva. Bekötve a Sofőr-riportba (`SoforScorecard.js`) is — a "Km (elmúlt 30 nap)" és "Átlagfogyasztás" oszlop **kizárólag tachográf-adatból** számol (a korábbi GPSmart-alapú km és jármű-szintű fogyasztás-számítás ebben a metódusban megszűnt, más oldalakon változatlan).

## Fájlok — központi fájlkezelővé bővítés (2026-07-23) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
A `Fájlok` admin oldal (`filesInterface.php::getFiles()` `tabla==="admin"` ága) már korábban is a teljes céges `fajlok`-állományt adta vissza. Ez a kör pótolta: (1) a Bank/MOL/Tachográf import mostantól a nyers fájlt is elmenti (`tabla` = `bank_import`/`mol_import`/`tachograf_import`, `rowid` = `ceg_id`); (2) feltöltő-nyomon követés (`fajlok.feltolto_tipus`/`feltolto_id`/`feltolto_nev`, denormalizált snapshot — nincs SQL join), fájltípus-kategorizálás (`fajl_kategoria`, a kiterjesztésből számolva), egyszerű vesszős címkézés (`cimkek`); (3) a feltöltési whitelist bővült (ppt/video/audio/zip/ddd) — **explicit felhasználói döntés**, mert ez korábban pont ezek kizárására szolgált RCE-védelemként (`backend/files/` a production webrootön belül van). `getFiles()` admin-ága opcionális szűrőkkel bővült (kategoria/modul/feltoltoId/dátum/rendezés).

## Fájlok — UI/UX audit és prémium redesign (2026-07-23) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
A `Fájlok` menüpont önálló, dashboard-stílusú modullá épült: `src/components/Fajlok/` (`FajlDashboardStats.js`/`FajlFilterChips.js`/`FajlGrid.js`/`FajlUploadZone.js`/`FajlPreviewPanel.js`/`fajlKategoriaInfo.js`/`FileTypeIcon.js`) — kizárólag `src/views/admin/Fajlok.js` használja, a megosztott `CardTableForFajlok.js`-t (amit 5 beágyazott hely is használ kisebb fájllistákhoz) visszaállítottuk egyszerű formájára. Kép-kategóriájú fájloknál valódi kicsinyített előnézet (`IntersectionObserver`-rel lusta betöltve), nem-kép fájloknál egy kategória-színű, kiterjesztés-feliratú jelvény (`FileTypeIcon.js`). Új backend actionök: `renameFile`, `downloadFilesZip` (PHP `ZipArchive`), `getStatisztika`, `getHasonloFajlok` (tisztán SQL-egyezés, nincs AI/LLM mögötte). Nézet-váltó (Grid/Table) `localStorage`-perzisztenciával. **Gotcha, ami újra előjöhet**: új Tailwind szín-osztály (pl. `bg-sky-500`) bevezetésekor mindig `grep`-eld a lefordított `tailwind.css`-t, ne csak a DOM-ot — a helyes class jelenléte a DOM-ban NEM garantálja, hogy a stílus ténylegesen létezik a `build:tailwind` nélkül.

## UI/UX audit és javítási kör (2026-07-23/24 + folytatás 2026-07-24) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
Egy 82 találatos, teljes admin-felületi UI/UX audit P0+P1 tétele (mind) és a P2 készlet nagy része megvalósult. Máig élő, újrahasznosítható infrastruktúra ebből a körből:
- **`utils/confirm.js` + `ConfirmDialogContainer.js`** — `confirmDialog(message, options)` Promise-alapú, `Modal.js`-re épülő dialógus váltotta le a natív `window.confirm()`-öt 19 fájlban (`options.danger`/`options.confirmLabel` testre szabható). Új törlés-megerősítésnél ezt használd, ne `window.confirm`-öt.
- **`components/UI/PrimaryButton.js`** — egységesített elsődleges gomb stílus (`text-xs font-bold uppercase tracking-wide`).
- **`components/UI/AdminBreadcrumb.js`** — `Csoport / Lista / Aktuális elem` sáv, mind a 7 `*Form.js` oldalon bekötve.
- **`components/UI/LejaratTag.js`** — piros "Lejárt"/borostyán "N nap múlva" jelvény, a jármű lejárati mezőkön (Kamion/Furgon/Pótkocsi), ugyanaz a küszöb, mint a Dashboard "Mire figyeljek ma"/naptár.
- **Valódi regresszió, amit ez a menet okozott és javított**: a `DataTable.js` dupla-klikk→egyszeri-klikk átállás után az "actions" oszlop gombjai a sor-navigációt IS kiváltották — a `<td>` `onClick`-je mostantól `e.stopPropagation()`-t hív. **Ha valaha újra a sor-klikk viselkedését módosítod, élőben ellenőrizd, hogy az akció-gombok külön kattinthatók maradnak** — csendben, csak interakció közben derül ki.
- Oszloprendezés bekapcsolva 6 CRUD modulon (Kamionok/Pótkocsi/Furgonok/Sofőrök/Ügyfelek/Helyszínek) + Sofőr-riport, ugyanazzal a `RENDEZHETO_OSZLOPOK` fehérlista-mintával, mint a Pénzforgalomnál.
- Pótkocsi kapott egy "Méret" mezőt (`potkocsi.meret`, `backend/sql/31.sql`-hez fűzve) — korábban hiányzott a Kamion/Furgon modulhoz képest.
- Szabadságok kapott valódi szerkesztés-funkciót (`updateSzabadsag`) — korábban csak létrehozás/törlés volt.

## Fuvar-dokumentum OCR + Fuvar modul (2026-07-25) — **SUPERSEDED 2026-07-28, ld. "Fuvar-first munkafolyamat" lentebb**

**A teljes OCR-alapú dokumentum-first folyamat (ez a szekció) 2026-07-28-án visszavonásra került** — a `beerkezett_dokumentumok`-ra épülő kód (Gemini OCR-hívás, entitás-egyeztetés, admin inbox, sofőr-oldali feltöltő) teljesen törölve, a `beerkezett_dokumentumok` DB-tábla érintetlenül, de használaton kívül maradt (ld. lentebb). Ez a szekció csak történeti kontextusként marad meg — a `fuvarok` tábla és a vele kapcsolatos admin CRUD (`FuvarInterface`, `Fuvarok.js`/`FuvarForm.js`) továbbra is él, csak a dokumentum-first létrehozási út szűnt meg.

Új, admin-only modul (**sub-project 1 of a planned 7** — Számlázz.hu-integráció, NAV kimenő-számla-követés, bank-egyeztetés fuvar-szinten, statisztikai dashboardok és a globális kereső bővítése még hátravan, ld. a teljes tervet: `docs/superpowers/specs/2026-07-25-fuvar-dokumentum-ocr-design.md`): a papíralapú fuvarlevél/szállítólevél dokumentumokból Gemini OCR-rel kinyert adatok alapján egy fuvar rögzítése 30-60 másodperc alatt legyen, a korábban (ld. "Removed: Fuvarok / Fuvartervező modulok" fentebb) tudatosan kivezetett, szóbeli/emlékezet-alapú fuvarnyilvántartás helyett — a különbség a régi modulhoz képest az, hogy az adatforrás most a fizikai dokumentum OCR-je, nem a diszpécser utólagos, gyakran elmaradó kézi rögzítése.

### Adatmodell: `beerkezett_dokumentumok` ↔ `fuvarok`

- **`beerkezett_dokumentumok`** — a feltöltött dokumentum + OCR-eredmény, mielőtt (vagy ha soha nem) fuvarrá válik: `fajl_id` → `fajlok.sorszam` (a nyers fájl), `tipus` (`fuvarlevel`/`szallitolevel`/`ismeretlen`, OCR-becsült, admin felülírhatja), `ocr_allapot` (`feldolgozatlan`/`kesz`/`hiba`), `ocr_adatok` (JSON, a kinyert mezők), `fuvar_id` (NULL, amíg nem lett belőle fuvar). Az admin inbox (`getBeerkezettDokumentumok`) alapértelmezetten csak a `fuvar_id IS NULL` sorokat listázza — ez a "feldolgozandó" nézet, nem egy `allapot` mező.
- **`fuvarok`** — a tényleges fuvar: `sofor_id`/`kamion_id`/`furgon_id` (kölcsönösen kizáró, mint a korábbi kamion/furgon-mintáknál)/`potkocsi_id`/`megbizo_id` (→ `ugyfelek.id`), útvonal (`felrako`/`lerako`/`tavolsag_km`), `fuvardij`/`egyeb_koltseg` (az "Összesen" sosincs tárolva, mindig `fuvardij + egyeb_koltseg` a lekérdezésben), `allapot` (5 érték: `rogzitett`/`szamlazasra_var`/`szamlazva`/`fizetesre_var`/`teljesitve` — **ebben a fázisban csak kézi váltással**, az automatikus váltás a Számlázz.hu/NAV-fázisokra vár). Egy `beerkezett_dokumentumok` sor és a belőle létrehozott `fuvarok` sor kapcsolata kétirányú: `beerkezett_dokumentumok.fuvar_id` mutat előre, a mögöttes `fajlok` sor pedig át van parentelve (ld. lentebb) — nincs duplikált fájltárolás.
- **`ugyfelek.fizetesi_hatarido_nap`** visszahozva (a korábbi Fuvarok-kivezetés törölte) — kizárólag ennek a modulnak a megbízó-auto-kitöltési igénye miatt, nem a régi fuvar-fizetettségi funkció feltámasztása.

### Gemini OCR-integráció (`backend/GeminiOcrClient.php`, `backend/interface/beerkezettDokumentumInterface.php`)

- **Modell: `gemini-3.5-flash`**, ingyenes kvóta-szinten, nyers curl-hívás (nem SDK — a projektnek nincs composer.json-ja). Multimodális LLM-et a kézzel írott (kurzív magyar) fuvarlevelek szemantikus kézírás-értelmezése indokolt — egy hagyományos OCR-motor ezt nem tudná megbízhatóan.
- **`maxOutputTokens: 8192` explicit beállítása kritikus**: a modell "thinking" változata a válasz-token-keretet a látható JSON ÉS a belső gondolkodási token-felhasználás közt osztja meg — enélkül a JSON-kimenet csendben, `finishReason: "STOP"` mellett (nem `MAX_TOKENS`-szel jelezve!) a közepén levágódott, amit `json_decode()` `null`-ra old fel — ez hamis "OCR sikertelen" állapotot produkált egy ténylegesen sikeres, csak túl szűkre szabott hívás után. Ez élő teszttel (Task 5) derült ki, nem kód-olvasással.
- **Kvóta/rate-limit — élőben, ismételten megfigyelt kockázat, nem csak elméleti**: a megosztott Gemini API-kulcsnak van napi ingyenes kvótája ÉS (ennél gyakrabban ténylegesen elért korlátként) egy percenkénti kérés-limitje. Ez a Task 16 golden-path-futtatása alatt is ténylegesen bekövetkezett: több egymást követő OCR-hívás rövid időn belül 2× is `ocr_allapot='hiba'`-t adott ~1 másodperces válaszidővel (szemben egy valódi, sikeres hívás ~3-13 másodperces "thinking"-latenciájával) — ez a gyors válaszidő volt a jele, hogy a hívás egy rate-limit-választ kapott, nem egy ténylegesen lefutott Gemini-elemzést. A retry (kis várakozás után) mindkétszer sikeres volt. **Ez elfogadott, jelenleg nem korlátozott kockázat** — nincs kliens-oldali throttling/backoff a `elemezBeerkezettDokumentum` actionön, és most szélesebb kör (admin ÉS sofőr sessionök is, ld. lentebb) hívhatja, mint eredetileg tervezve volt.
- **Graceful degradation hibán**: sikertelen OCR (kvóta/hálózati hiba/csonka válasz) esetén a feltöltés **mindig perzisztálódik** (`fajlok` sor + `beerkezett_dokumentumok` sor `ocr_allapot='hiba'`, `ocr_adatok=NULL`), admin kézzel tölti ki a mezőket — ez **tudatos eltérés** a Bank/MOL/Tachográf import mintájától (ott hiba esetén semmi nem perzisztálódik), mert itt kifejezett elvárás, hogy egy sofőr-feltöltés sose vesszen el egy átmeneti Gemini-hiba miatt.
- **Saját cégnév disambiguáció (`admin.cegnev`, új mező)**: az első élő teszten a modell összekeverte a dokumentum nyomtatott fejlécén szereplő saját cégünket (a fuvarozót) a tényleges megbízóval. Mivel a rendszer több tenant-ot szolgál ki, a saját cégnév nem írható fixen a promptba — a `admin.cegnev` (csak a root/tulajdonos admin sorában töltendő ki) szerver-oldalon, `ceg_id`-ből feloldva kerül bele a promptba minden híváskor, egy explicit szabállyal ("a nyomtatott fejléc cége SOSEM lehet megbízó"). Ugyanez a mező a jövőbeli Számlázz.hu-fázisban is kellhet (kibocsátó adatai a számlán).

### `letrehozFuvarDokumentumbol()` — fájl-reparenting

Fuvar dokumentumból történő létrehozásakor a mögöttes `fajlok` sor **nem másolódik**, hanem áthelyeződik: `UPDATE fajlok SET tabla='fuvar', rowid=<új fuvar id> WHERE sorszam = <fajl_id>` — ezután a dokumentum a fuvar szokásos melléklet-galériájában jelenik meg, ugyanazzal a komponenssel, mint minden más modulnál. Ez egy a kódbázisban korábban nem használt, de egyszerű minta (egyetlen UPDATE, nem új infrastruktúra). **Gotcha, amibe menet közben belefutottunk (Task 14)**: ha egy `letrehozFuvarDokumentumbol()`-lel létrehozott fuvart utólag, teszteléskor törölnek, a hozzá tartozó `fajlok` sor `tabla='fuvar'`/`rowid=<törölt fuvar id>`-n marad — **nem áll vissza automatikusan** `beerkezett_dokumentum`-ra, mert a törlés (`deleteFuvar`) csak a `fuvarok` sort soft-deleli, a `fajlok` reparentinget nem vonja vissza. Task 16 golden-path-futtatása során egy pontosan ilyen, korábbi menetből (Task 14) itt maradt dangling `fajlok` sort találtunk és takarítottunk el (`tabla='fuvar'`, `rowid`= egy már nem létező `fuvarok.id`-ra mutatva) — ha egy jövőbeli teszt-fuvart dokumentumból hoznak létre és utána törölnek, érdemes explicit ellenőrizni/visszaállítani a hozzá tartozó `fajlok` sort is, nem csak a `fuvarok` sort.

### `AutocompleteSelect` — első saját típusahasú komponens a kódbázisban

`src/components/UI/AutocompleteSelect.js` (`FuvarForm.js` Sofőr/Jármű/Pótkocsi/Megbízó mezői) az első típusahead-autocomplete komponens ebben a kódbázisban — korábban minden választó mező plain `<select>` volt. Szöveges kereséssel szűrt legördülő lista, kiválasztás után egy "chip"-szerű (kitöltött mező + "Kiválasztás törlése" gomb) állapotra vált. **Jövőbeli entitás-választó mezőknél ezt érdemes újrahasznosítani egy sima `<select>` helyett**, különösen ha a választható lista hosszú lehet (jelenleg csak a Fuvar modul él vele, de nincs oka, hogy ez maradjon az egyetlen fogyasztója).

### Schema-drift gotcha (Task 7) — `CREATE TABLE IF NOT EXISTS` csendben no-op-olt egy elavult táblán

A `fuvarok` táblának egy régebbi, teljesen más sémájú (`ugyfel_id`/`felrakas_cim`/`dij`/`statusz` oszlopokkal, egy korábbi, nem ehhez a feature-höz tartozó munkamenetből maradt) verziója már létezett a helyi dev DB-ben, amikor Task 1 `backend/sql/33.sql`-je lefutott. MySQL `CREATE TABLE IF NOT EXISTS`-a **csak a tábla létezését ellenőrzi, az oszlop-szerkezetet nem** — a migráció csendben, hiba nélkül no-op-olt, és a valódi (`sofor_id`/`kamion_id`/.../`allapot` oszlopos) séma sosem jött létre. Ez csak akkor derült ki, amikor Task 7 (`FuvarInterface`) élesben lefuttatta az első lekérdezést az elvárt oszlopok ellen, és `SQLSTATE[42S22]: Column not found` hibát kapott. Fix: `backend/sql/34.sql` (új fájl, mert `33.sql` már commitolva volt) droppolta és a helyes sémával újra létrehozta a táblát. **Általános tanulság, minden jövőbeli `CREATE TABLE IF NOT EXISTS`-alapú migrációra**: ha egy tábla névileg már létezhet egy korábbi, nem kapcsolódó munkamenetből, egy puszta "sikeresen lefutott, a tábla létezik" ellenőrzés (ahogy Task 1 saját verifikációja is tette) nem elég — a tényleges oszlop-szerkezetet (`SHOW COLUMNS`) is össze kell vetni az elvárt sémával, különben a hibát csak egy jóval későbbi, a táblát ténylegesen használó lekérdezés fedi fel.

### `fajlok.sorszam`, nem `.id` — ismét megerősítve

A `fajlok` tábla elsődleges kulcsa `sorszam`, nem `id` (ld. "Fájlok — központi fájlkezelővé bővítés" fentebb) — ez a Fuvar modulban két helyen okozott volna csendes hibát, ha nem kezelik explicit: `beerkezettDokumentumInterface::fajlnevekFeloldasa()` (`fajl_id IN (...)` ellen `SELECT sorszam, filename FROM fajlok`, nem `id`-t) és `letrehozFuvarDokumentumbol()`-nek a fenti reparenting-UPDATE-je (`WHERE sorszam = <fajl_id>`). Mindkét helyen expliciten kikommentezve, hogy jövőbeli módosításnál ne csússzon vissza `id`-ra.

### No-JOIN / no-UNION konvenció újra megerősítve

A projekt saját SQL-lintere sem `JOIN`-t, sem `UNION`-t nem enged — a Fuvar modul minden entitás-név-alapú keresése/dúsítása (`FuvarInterface::keresIdkNevAlapjan()`/`dusitSorokat()`/`batchLekerdezes()`, sofőr/kamion/furgon/pótkocsi/megbízó nevek és rendszámok feloldása egy `fuvarok` lekérdezéshez) külön `SELECT`+PHP-oldali `IN (...)`/merge mintát követ, ugyanúgy, mint a korábban dokumentált `helyszinInterface::hozzafuzMegjegyzesekSzama()`/`bejelentesekInterface::getUzenetInfok()`.

### Cross-tenant FK-tulajdonjog ellenőrzés `newFuvar`/`updateFuvar`-on

Az első implementáció (Task 7) client-submitted `sofor_id`/`kamion_id`/`furgon_id`/`potkocsi_id`/`megbizo_id` mezőket kötött be közvetlenül INSERT/UPDATE-be, ellenőrzés nélkül — ez egy cross-tenant IDOR lett volna (egy admin egy MÁSIK cég sofőrjét/járművét/ügyfelét is hozzárendelhette volna egy saját fuvarhoz, ha kitalálja/megsejti az id-t). Fix (még Task 7 alatt, review-találatból): minden nem-üres FK id-t `ervenyesEntitasE($tabla, $id, $ceg_id)` ellenőriz írás előtt (`WHERE id = :id AND admin = :ceg_id AND torolt <> 'I'`), plusz olvasáskor is (`egyMezoLekerdezese()`/`batchLekerdezes()`) `AND admin = :ceg_id` védelem — ugyanaz a kétrétegű (write-time + read-time) minta, mint a 2026-07-19-i biztonsági audit óta a többi modulnál.

### `MODULE_PERMISSION_MAP` tudatos kompromisszum — `elemezBeerkezettDokumentum` admin-szerepkör-ellenőrzés nélkül

`elemezBeerkezettDokumentum`-ot eredetileg (Task 5) a `MODULE_PERMISSION_MAP`-ban `['fuvarok', 'szerkesztes']`-ként regisztrálták, admin-only feltételezéssel. Amikor a sofőr-oldali fuvarlevél-feltöltő (Task 15) implementációja alá ért, kiderült: ez az action mindkét munkamenet-típusból (admin ÉS sofőr) hívandó, de `requirePermission()` saját maga is `resolveKerelmezo()`-t hív a `validation()` lépésben, MIELŐTT a case-ág egyáltalán lefutna — tehát a case-ágban való `resolveSajatCegId()`-re váltás önmagában nem lett volna elég, a `MODULE_PERMISSION_MAP`-bejegyzés a sofőr-hívást már a validáció szintjén elvéreztette volna. **A fix a `MODULE_PERMISSION_MAP`-bejegyzés teljes eltávolítása volt** (nem csak egy admin/sofőr-ág hozzáadása hozzá), pontosan ugyanaz a minta, mint a többi, mindkét szerepkörből hívható action (`fileUpload`, `getHelyszinek`, `globalSearch`) esetén, amik szintén hiányoznak ebből a map-ből.

**Explicit, szándékosan dokumentált mellékhatás (a review kifejezetten kérte, hogy ez itt, nem csak a review-jegyzőkönyvben szerepeljen)**: ez azt is jelenti, hogy a per-szerepkör, admin-configurálható `fuvarok`/`szerkesztes` jogosultság (a `Jogosultságok` oldalon) **ettől az EGY konkrét actiontől kezdve nem érvényesül** admin/csapattag hívóknál sem — bármely hitelesített admin-csapattag ki tudja váltani az OCR-elemzést + inbox-beszúrást, függetlenül a saját, konfigurált Fuvarok-modul jogosultságától. A `letrehozFuvarDokumentumbol`, `getBeerkezettDokumentumok` és `updateBeerkezettDokumentumTipus` viszont **továbbra is helyesen jogosultság-ellenőrzött** marad. Ez egy tudatos, alacsony súlyosságú kompromisszum — az action legfeljebb egy review-only inbox-bejegyzést hoz létre, sosem egy valódi `fuvarok` üzleti rekordot —, de expliciten dokumentálva van, hogy egy jövőbeli `MODULE_PERMISSION_MAP`-diffelő ne kelljen újra levezetnie az indoklást.

### Golden-path végigfuttatás (Task 16) — élőben, mindkét szerepkörrel (admin+sofőr) tesztelve
Teljes folyamat Playwright-tal ellenőrizve: sofőr feltöltés → admin inbox+OCR → fuvar létrehozás dokumentumból (rendszám-bizonytalanság kézi javításával) → lista/keresés/rendezés/állapotváltás → törlés. Az egyetlen felmerült hiba a fent dokumentált Gemini rate-limit volt, nem kód-hiba. Teljes jegyzőkönyv: `CLAUDE-history.md`.

## Fuvar-modul kiegészítések a "next steps" listából (2026-07-25/26) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
- **Számlaszám-hozzárendelés Számlázz.hu API-integráció HELYETT** — nincs API-hívás, az admin a Számlázz.hu felületén saját maga állít ki számlát, ide csak a számlaszámot írja be utólag, kézzel, tömegesen kijelölt fuvarokra (N:1 kapcsolat). Ld. memória `project_szamlazz_hu_terv`.
- **NAV kimenő-számla-követés szándékosan kihagyva** — felhasználó explicit kérésére. Ne javasold alapból egy jövőbeli körben. Ld. memória `project_nav_kimeno_kihagyva`.
- **Bank-egyeztetés fuvar/számla-szinten** — a Bank CSV import review-táblázatában javasolt sor jelenik meg, ha a közlemény tartalmaz egy `szamlazva`/`fizetesre_var` fuvar számlaszámát; elfogadáskor a fuvar `teljesitve`-re vált.
- **Globális kereső (Ctrl+K) bővítve fuvarokkal**, **statisztikai dashboardok** (`getFuvarStatisztikak`, sofőr/jármű/megbízó/havi/pénzügyi bontás), **figyelmeztető sáv** (`getFuvarFigyelmeztetesek`, `FigyelmeztetesSav.js`) — **tudatosan nincs automatikus állapotváltás** (a workflow-automatizálás ötletét a felhasználó elvetette), csak jelző lista. A figyelmeztetés-sáv "Megnyitás" gombja a fuvar **`felrako`** mezőjére tölti elő a keresőt, nem a megjelenített "A → B" útvonal-stringre (ez élő teszttel felfedezett, javított hiba volt — a backend soronként, egy-egy oszlopra LIKE-ol).

## Jármű teherbírás, Pénzforgalom Útdíj kategória, fuvarozási profit-riport (2026-07-26) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
- `kamion`/`furgon`/`potkocsi` mind kapott egy `teherbiras DECIMAL(6,2) NULL` (tonna) oszlopot.
- Pénzforgalom 5. kategóriája: **"Útdíj"** (`utdij`) — a `karbantartas`/`biztositas`/`ber` mintától eltérően **nem admin-only és nem letiltott** a kategória-szűrőn, mert nincs más adatforrása, mint az `egyeb_koltsegek` lista.
- Fuvarok "Statisztikák" fülén fuvarozási profit-riport (bevétel a `fuvarok` táblából, kiadás üzemanyag+útdíj+bér). **Fontos, hogy a Fuvar-bevétel és a Pénzforgalom-bevétel SZÁNDÉKOSAN nem egyezik** — a fő Pénzforgalom "Nettó eredmény" bevétele kizárólag a manuálisan/NAV-ból importált `egyeb_koltsegek` sorokból jön, sosem a `fuvarok` táblából (ld. "Removed: Fuvarok" fentebb). Ne tekintsd hibának, ha a két szám eltér.
- **Gotcha**: a Fuvarok "Statisztikák" fület egy ETTŐL A TERVTŐL FÜGGETLEN, korábbi commit ideiglenesen lecserélte egy "Sofőr szerint" nézetre — végül mindkettő megmaradt (`nezetMod`: `tablazat`/`kanban`/`sofor`/`statisztika`). **`/admin/fuvarStatisztika` (`FuvarStatisztika.js`, `getSoforDashboard`-ra épülő sofőr/dokumentum-trend dashboard) egy TELJESEN KÜLÖNÁLLÓ oldal** — ne keverd össze ezzel a "Statisztikák" nézet-füllel.

## NAV számla teljesítés/fizetési határidő, és Pénzforgalom dátum-alap váltó (2026-07-26) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
`egyeb_koltsegek` kapott `teljesites_datum`/`fizetesi_hatarido` DATE NULL oszlopot (NAV digest-ből is töltve, kézzel is szerkeszthető, `LejaratTag`-gel jelezve). A Pénzforgalom fejlécén egy "Dátum alapja" szelektor (`datum`/teljesítés/fizetési határidő) szűri a Tételek listát/KPI-sort/kategória-rácsot/Jármű-bontást/Havi alakulást — **csak** a kézzel rögzített/importált `egyeb_koltsegek` sorokra hat, a karbantartás/tankolás/biztosítás/bér mindig saját `datum` szerint szűrődik (nincs más dátumuk). **Gotcha, élő teszttel felfedezve**: a `DatePicker.js` szintetikus `onChange`-eseményének nincs `setCustomValidity` metódusa — egy feltétel nélküli `e.target.setCustomValidity("")` hívás emiatt TypeError-ral elhasalt minden dátummező-kattintásnál (a "Dátum" mező is érintett volt, nem csak az új kettő) — fix: `e.target.setCustomValidity?.("")`. Ha egy jövőbeli form-onChange handler `DatePicker`-ből jövő szintetikus eseményt kezel, mindig opcionális láncolással hívj rajta DOM-only metódusokat.

### Sidebar "napi zóna" testreszabás (2026-07-26) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
A desktop sidebar "napi zóna" felhasználónként testreszabható (korábban 6, kódban rögzített menüpont). `PIN_REGISTRY` (`Sidebar.js` teteje) katalogizálja a kitűzhető elemeket; a kiválasztás `localStorage`-ban (`sidebar-pins-${user.id}`) perzisztál, max 8 elem. `NapiZonaEditorModal.js` a szerkesztő UI. Csak a desktop sidebart érinti, mobil nav változatlan. **Tudatosan elfogadott drift-kockázat**: a `PIN_REGISTRY` egy harmadik, kézzel karbantartott nav-forrás a mobil `mobileGroups` és a hardcoded desktop csoport-JSX mellett — route átnevezésnél/törlésnél mindhárom helyet frissíteni kell.

## Sofőr dokumentum-feltöltési előzmény + Dashboard átrendezés (2026-07-26) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
Két új sofőr-önkiszolgáló backend action (`getSajatBeerkezettDokumentumok`/`torolSajatBeerkezettDokumentum`) — a sofőr csak a SAJÁT, még fuvarrá nem alakított (`fuvar_id IS NULL`) feltöltéseit láthatja/törölheti, semmilyen betekintése nincs az OCR-eredménybe vagy a fuvar-összekapcsolásba. A Dashboardon a "Dokumentum" csempe kiemelt, brand-színű kártyává vált (mini-előnézettel), a "Legutóbbi bejelentéseim" szekció ehhez képest lezsugorodott. A BottomNav piros "Bejelentés" FAB-ja szándékosan változatlan.

## Fuvar OCR-bővítés: távolság (km) + tömeg (kg) (2026-07-26) — összefoglaló, teljes jegyzőkönyv: `CLAUDE-history.md`
A Gemini OCR JSON-sémája kapott két numerikus mezőt (`tavolsag_km`/`tomeg_kg`, tonna→kg váltással). `fuvarok.tomeg_kg DECIMAL(8,2) NULL` (`backend/sql/41.sql`), `FuvarInterface::letrehozDokumentumbol()` átveszi mindkettőt, `FuvarForm.js` "Útvonal" szekciója kapott egy "Tömeg (kg)" mezőt. Az OCR-séma `suly` mezője ezzel érdemben kiváltva, de a mező maga a sémában maradt (alacsony kockázatú holt mező).

## Beérkezett dokumentum OCR — visszaállítva szinkron feldolgozásra (2026-07-27)

Egy 2026-07-27-i kör rövid ideig kétfelé bontotta ezt a feldolgozást: az
`elemezBeerkezettDokumentum` csak a fájlt mentette + a sort hozta létre
`ocr_allapot='feldolgozatlan'`-ra, egy külön `ApiHandler::inditsBackgroundOcr()`
pedig `exec("nohup $php $script $id > /dev/null 2>&1 &")`-vel indított egy
elszakított PHP-CLI processzt (`backend/cli/ocr_feldolgozas.php`), ami
ténylegesen elvégezte a Gemini-hívást. Élesben ez nem működött megbízhatóan,
ezért **ez a kör teljesen visszaállt a korábbi, szinkron verzióra**: az
`elemezBeerkezettDokumentum` action ismét a HTTP-kérésen BELÜL, közvetlenül
hívja a Gemini OCR-t (`BeerkezettDokumentumInterface::elemez()`), a válasz a
modell "thinking"-latenciájának megfelelően (3-13+ másodperc) érkezik. A
háttérfolyamatos kísérlet minden darabja (`backend/cli/ocr_feldolgozas.php`,
`ApiHandler::inditsBackgroundOcr()`/`ujraprobalBeerkezettDokumentumOcr` action,
a `DokumentumReviewPanel.js` "Újrapróbálás" gombja, a sofőr-oldali
`DokumentumFeltoltes.js` kliens-poll, a `BeerkezettDokumentumok.js` kézi
"Frissítés" gomb, a `DokumentumKartya.js` 3-állapotú — feldolgozatlan/hiba/kész
— jelvény-logikája) el lett távolítva.

**Nyitott, még nem megoldott probléma**: a szinkron hívás válaszideje (a
felhasználó eredeti panasza) így visszatért — ez volt az aszinkron kísérlet
kiváltó oka. A háttérfolyamatos megoldást a `PHP_BINARY`-alapú SAPI-gotcha
(ld. korábbi git history, `git show cad434a`/`b2acd45`) és/vagy egy még nem
azonosított hiba miatt a felhasználó élesben működésképtelennek találta —
mielőtt bármilyen aszinkron/háttérfolyamatos megoldást újra megpróbálnál,
tisztázd vele, mi pontosan romlott el (a HTTP-kérés maga hibázott, a
háttér-processz sosem indult el, vagy valami más), mert ez a kör explicit
kérésre visszaállt a régi viselkedésre ahelyett, hogy ezt kideríteni
próbálta volna.

## Fuvar létrehozása — Sofőr/Jármű/Pótkocsi/Megbízó előzetes kitöltése + OCR távolság/tömeg megjelenítése (2026-07-28) — **SUPERSEDED ugyanaznap, ld. "Fuvar-first munkafolyamat" lentebb**

**Ez a teljes kiegészítés (`egyeztetOcrAlapjan()`, `getEgyeztetesJavaslatDokumentumhoz()`, `getFuvarEgyeztetesJavaslat` action, a `FuvarForm.js` OCR-előtöltő `useEffect`-je) ugyanezen a napon, néhány óra múlva törlésre került**, amikor a Fuvar modul dokumentum-first folyamata teljesen megfordult fuvar-first-re (ld. lentebb) — dokumentum-first folyamat nélkül nincs mit előzetesen egyeztetni, a fuvar mezői mindig kézzel/admin által kerülnek be, elsőként. Ez a szekció csak történeti kontextusként marad meg.

Két kis kiegészítés a Fuvar-dokumentum OCR modulhoz (ld. "Fuvar-dokumentum OCR + Fuvar modul" fentebb):

- **`FuvarInterface::egyeztetOcrAlapjan($ceg_id, $ocrAdatok)`** — az OCR-rendszám/sofőr-név/megbízó-név → entitás-id egyeztetést, amit korábban KIZÁRÓLAG `letrehozDokumentumbol()` futtatott le, MENTÉSKOR, most egy külön, újrafelhasználható privát metódusba emeltük ki. Új publikus, csak-olvasó metódus: **`getEgyeztetesJavaslatDokumentumhoz($dokumentumId, $ceg_id)`** (új API action: `getFuvarEgyeztetesJavaslat`, `['fuvarok', 'hozzaferes']` jogosultsággal) — a `FuvarForm.js` ezt hívja meg a form megnyitásakor (`dokumentumId` jelenlétekor), hogy a Sofőr/Jármű/Megbízó mezők már kitöltve jelenjenek meg, ne csak mentéskor, láthatatlanul dőljenek el. Csak az üres mezőket tölti ki (`prev.x || javaslat.x`), nem írja felül a felhasználó saját szerkesztését.
- **Pótkocsi-javaslat, ÚJ logika**: az OCR-ből nincs külön pótkocsi-rendszám mező (csak a vontató jármű rendszáma), ezért a pótkocsi-javaslat a felismert **kamion SAJÁT `potkocsi` FK-jából** jön (`potkocsiIdKamionhoz()`, a szokásos cross-tenant `ervenyesEntitasE()` védelemmel — egy admin/potkocsi-pár tenant-mismatch esetén `null`-t ad, nem egy másik cég pótkocsiját). Csak kamionnál van ilyen javaslat, furgonnál sosem (furgon nem vontat). **Ez a `letrehozDokumentumbol()`-ban is megváltoztatta a viselkedést**: eddig egy dokumentumból létrehozott fuvar `potkocsi_id`-je mindig `NULL` maradt, hacsak az admin kézzel be nem állította — mostantól a fenti javaslat automatikusan bekerül (felülírhatóan) a mentéskor is.
- **A `DokumentumReviewPanel.js` dl-blokkja megjeleníti az OCR által felismert `tavolsag_km`/`tomeg_kg` mezőt is** (eddig csak felrakó/lerakó/megbízó/dátum/rendszám látszott, holott a `fuvarok.tavolsag_km`/`tomeg_kg` oszlopok és az OCR-séma már 2026-07-26 óta léteznek, ld. "Fuvar OCR-bővítés: távolság + tömeg" fentebb).
- Élőben tesztelve (helyi DB-be szúrt teszt `beerkezett_dokumentumok` sorral, Playwright-tal): a review-panel helyesen mutatja a távolságot/tömeget, a "Fuvar létrehozása" form pedig helyesen előtölti mind a négy mezőt (Sofőr/Jármű/Pótkocsi/Megbízó), beleértve a "Korábbi fuvarok ezzel a megbízóval" panel automatikus betöltését is.

## Fuvar-first munkafolyamat + sofőr push/feltöltés (2026-07-28) — teljes terv: `docs/superpowers/specs/2026-07-28-fuvar-first-workflow-design.md`, implementációs terv: `docs/superpowers/plans/2026-07-28-fuvar-first-workflow.md`

A Fuvar modul iránya **megfordult**: a fuvarszervező (admin) hozza létre előre a fuvart (sofőrrel/járművel már hozzárendelve), a sofőr push-értesítést kap róla, és utólag tölti fel a menetlevél (kötelező) és a szállítólevél (opcionális) fotóját ahhoz a konkrét fuvarhoz. A korábbi OCR-alapú, dokumentum-first folyamat (ld. a fenti két SUPERSEDED szekció) **teljesen megszűnt** — nincs többé Gemini OCR-hívás, entitás-egyeztetés vagy admin inbox; a fotó csak igazolás, a fuvar adatai már megvannak, mielőtt a kép elkészül.

- **`fuvarok.dokumentum_feltoltve DATETIME NULL`** — az ELSŐ menetlevél-feltöltéskor `NOW()`-ra áll (`FuvarInterface::allitDokumentumFeltoltve()`, idempotens). **Szándékosan független `allapot`-tól** — a sofőr "aktív fuvarjaim" listája (`getSajatFuvarok`) `dokumentum_feltoltve IS NULL AND allapot <> 'teljesitve'` alapján szűr: a kettő bármelyike, EGYMÁSTÓL FÜGGETLENÜL lezárja a fuvart a sofőr szemszögéből (élőben tesztelve mindkét út — sofőr feltölt, ill. admin `teljesitve`-re vált dokumentum nélkül — mindkettő helyesen eltünteti a fuvart az aktív listából).
- **Sofőr-oldali dokumentum-fotók — nincs új tábla.** A meglévő polimorf `fajlok` tábla veszi fel őket (`tabla='fuvar'`, `rowid=<fuvar id>`), a `cimkek` oszlop `'menetlevel'`/`'szallitolevel'` értékkel (rögzített konvenció, csak erre az egy feltöltési útvonalra — a Fájlok modul általános, szabad címkézése máshol változatlan). Admin-oldalon ugyanezek a fájlok a már meglévő `CardFuvarFajlok.js`-ben jelennek meg, módosítás nélkül.
- **Ownership-gap, amit a generikus `FilesInterface::fileUpload()`/`getFiles()` NEM fed le**: ezek csak céges (`ceg_id`) szinten szűrnek, egy adott fuvar SOFŐR-tulajdonjogát nem ismerik — egy sofőr enélkül egy MÁSIK sofőr fuvarjához is tudna fotót csatolni/olvasni ugyanannál a cégnél. Ezért három dedikált, ownership-ellenőrzött action született (`feltoltFuvarDokumentumot`/`torolSajatFuvarDokumentumot`/`getSajatFuvarDokumentumai`), mindegyik előbb `FuvarInterface::getSajatFuvar($fuvarId, $soforId, $cegId)`-vel ellenőrzi, hogy a fuvar TÉNYLEG a hívóé, mielőtt a generikus primitívet hívná. Élőben tesztelve: egy másik sofőr fuvarjára való feltöltési kísérlet helyesen elutasítva.
- **Törlés nem "nyitja vissza" a fuvart**: `torolSajatFuvarDokumentumot()` egy már lezárt fuvarnál a `dokumentum_feltoltve`-t NEM állítja vissza NULL-ra még akkor sem, ha az épp törölt fotó volt az egyetlen menetlevél-kép — élőben megerősítve.
- **Push-generalizálás admin+sofőr címzettre**: `push_feliratkozasok` `admin_id` oszlopa `felhasznalo_tipus ENUM('admin','sofor')` + `felhasznalo_id` párra váltott (ugyanaz a minta, mint `beerkezett_dokumentumok.feltolto_tipus`/`feltolto_id`-nál). `PushInterface::sendPushSofornak()` új, a meglévő `sendPushAdminnak()` mellett. `savePushFeliratkozas`/`deletePushFeliratkozas`/`getPushStatusz` mostantól `requireValidSession()`-t hív `resolveKerelmezo()` (admin-only) helyett — dual-role action, ezért NINCS `MODULE_PERMISSION_MAP`-bejegyzése (ugyanaz a tanulság, mint korábban `elemezBeerkezettDokumentum`-nál: `requirePermission()` a `validation()`-ben admin-only `resolveKerelmezo()`-t hívna, mielőtt a case-ág eldönthetné, melyik szerepkörről van szó). `PushFeliratkozas.js` (már létező, admin `Settings.js`-en használt komponens) változtatás nélkül működik a sofőr `Profil.js`-en is.
- **Push-küldés fuvar-hozzárendeléskor**: `newFuvar` mindig küld, ha van sofőr; `updateFuvar` az UPDATE ELŐTTI `sofor_id`-t hasonlítja az újhoz, és CSAK akkor küld, ha ténylegesen ÚJ (nem üres) sofőrre került a fuvar — a leváltott sofőr nem kap semmit (explicit felhasználói döntés). Élőben tesztelve (fake push-feliratkozással): a küldési kísérlet ténylegesen lefut, a WebPushSender hívás/hiba-logolás helyesen történik.
- **Sofőr-oldali UI**: `BottomNav.js` középső FAB-ja Bejelentésről Fuvarokra váltott (ikon + CÉL + SZÍN is — a piros `bg-red-600`/`text-red-600` brand-kékre váltott, mert Fuvarok rutinszerű, nem vészhelyzeti művelet). **Ezzel a Bejelentés elvesztette az "egy érintésre, bárhonnan" tulajdonságát** (tudatos, dokumentált trade-off, nem hiba) — továbbra is elérhető a Dashboard "Legutóbbi bejelentéseim" során (VÁLTOZATLAN maradt) és a `/user/bejelentesek` oldalon. Új oldalak: `Fuvarok.js` (`/user/fuvarok`, "Aktívak"/"Lezártak" fül-pár), `FuvarReszletek.js` (`/user/fuvarReszletek`, read-only operatív adatok — SOSEM fuvardíj/egyéb költség/számlaszám/állapot — + két feltöltő szekció). A régi `/user/dokumentum-feltoltes` + `DokumentumFeltoltes.js` törölve.
- **`FuvarReszletek.js` push-deep-link kompatibilis**: egy push-értesítésből érkező kattintás sima URL-t nyit (a service worker-nek nincs React Router state-je, amit átadhatna) — az oldal `location.state?.data`-t használ gyors útként (lista felől navigálva, nincs extra lekérdezés), `?id=` query paramétert pedig fallbackként (`getSajatFuvar()`-ral frissen lekérdezve). Élőben mindkét belépési út tesztelve.
- **Dashboard.js**: a korábbi "Dokumentum feltöltése" brand-kártya helyén (pontosan azon a vizuális súlyon) most "Aktív fuvarjaim" áll (max. 3 sor + "Összes fuvarod" link 3 felett), state-tel navigálva `FuvarReszletek`-re (nincs extra hálózati kör).
- **Admin-oldal**: `Fuvarok.js`-ből eltűnt a halott "N dokumentum feldolgozásra vár" gomb (a törölt `/admin/beerkezettDokumentumok`-ra mutatott); `CardTableForFuvarok.js` kapott egy "Dokumentum" oszlopot (`StatusBadge` "Feltöltve"/"Hiányzik", semleges `neutral`/`success` tónussal, nem `warning` — pusztán informatív jelzés). A Sidebar HÁROM külön karbantartott nav-forrásából (mobil `mobileGroups` config, hardcoded desktop `<NavItem>` JSX, `PIN_REGISTRY` — utóbbi automatikusan levezetett) mindegyikből eltávolítva a "Beérkezett dokumentumok" bejegyzés.
- **DB-migráció**: `backend/sql/42.sql` — root MySQL-felhasználóval (`mysql -u root`) futtatva, mert az app saját `kamion` DB-felhasználója (ld. "Fejlesztési audit alapján megvalósított batch" fentebb, `29.sql` InnoDB-migráció után) csak SELECT/INSERT/UPDATE/DELETE/LOCK TABLES jogú, `ALTER TABLE`-hez nem elég.

## Workflow notes for Claude Code

- For any UI/frontend change, verify it by actually running it (`npm start` and/or `php8.2 -S localhost:8001` as needed, opening it in a browser, screenshotting/clicking through the changed flow) before reporting the task done — don't rely on code review or lint alone.
- Do this without asking for permission first. Starting a local dev server / PHP built-in server on this machine is low-risk and reversible — just start it and verify.
- After changing any Tailwind class usage, rebuild `tailwind.css` (see the `build:tailwind` note above) before verifying in the browser, otherwise new classes won't be present yet.
